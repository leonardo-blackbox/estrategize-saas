/**
 * FormPublicoPage — thin page wrapper.
 * All logic lives in features/form-publico/.
 */
import { FormPublicoAggregator } from '../../features/form-publico';

export default function FormPublicoPage() {
  return <FormPublicoAggregator />;
}
