import type {
  ApplicationField,
  ThemeConfig,
  FormSettings,
  FieldOption,
} from '../../api/applications.ts';

export type { ApplicationField, ThemeConfig, FormSettings, FieldOption };

export type Direction = 'forward' | 'back';
