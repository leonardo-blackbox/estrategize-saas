---
phase: 03-admin-cursos
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - backend/src/database/migrations/025_lesson_status.sql
  - backend/src/routes/admin/courses.ts
  - frontend/src/api/courses.ts
  - frontend/src/features/admin/components/AdminCursoDetailLessons/LessonPublishButton.tsx
  - frontend/src/features/admin/components/AdminCursoDetailLessons/AdminCursoDetailLessons.tsx
  - frontend/src/features/admin/components/AdminCursoDetailPage/AdminCursoDetailPage.tsx
autonomous: true
requirements: [ADMN-02]

must_haves:
  truths:
    - "Admin publica ou despublica aula com 1 clique a partir do detalhe do curso"
    - "Aulas criadas comecam como draft por padrao"
    - "Status da aula e visivel na lista de aulas do modulo"
  artifacts:
    - path: "backend/src/database/migrations/025_lesson_status.sql"
      provides: "Coluna status na tabela lessons"
      contains: "status"
    - path: "backend/src/routes/admin/courses.ts"
      provides: "Endpoints POST publish/unpublish para lessons"
    - path: "frontend/src/features/admin/components/AdminCursoDetailLessons/LessonPublishButton.tsx"
      provides: "Botao de publicar/despublicar aula"
      min_lines: 15
  key_links:
    - from: "LessonPublishButton.tsx"
      to: "/api/admin/courses/lessons/:id/publish"
      via: "mutation fetch POST"
      pattern: "lessons.*publish"
    - from: "AdminCursoDetailLessons.tsx"
      to: "LessonPublishButton.tsx"
      via: "props lesson.status + callbacks"
      pattern: "LessonPublishButton"
---

<objective>
Adicionar suporte a publicacao/despublicacao de aulas individuais no admin.

Purpose: ADMN-02 exige que o admin publique aulas com 1 clique. A tabela lessons nao tem campo status, e nao existem endpoints para alterar o status de aulas.
Output: Migration adicionando status em lessons, endpoints publish/unpublish, e botao no frontend.
</objective>

<execution_context>
@/Users/leonardorodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leonardorodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-admin-cursos/03-01-SUMMARY.md

<interfaces>
<!-- Tabela lessons (migration 006) — sem campo status -->
create table public.lessons (
  id             uuid primary key default uuid_generate_v4(),
  module_id      uuid not null references public.modules(id) on delete cascade,
  title          text not null,
  description    text,
  video_url      text,
  duration_secs  integer,
  sort_order     integer not null default 0,
  is_free_preview boolean not null default false,
  drip_days      integer not null default 0,
  created_at     timestamptz not null default now(),
  updated_at     timestamptz not null default now()
);

<!-- Backend lessonSchema existente -->
const lessonSchema = z.object({
  title: z.string().min(1).max(200),
  description: z.string().optional(),
  video_url: z.string().url().optional().nullable(),
  duration_secs: z.number().int().min(0).optional().nullable(),
  is_free_preview: z.boolean().optional(),
  drip_days: z.number().int().min(0).optional(),
});

<!-- AdminCursoDetailLessons props (54 linhas, limite 80) -->
interface AdminCursoDetailLessonsProps {
  lessons: any[];
  confirmDeleteLesson: string | null;
  deleteLessonMutation: { mutate: (id: string) => void; isPending: boolean };
  onEditLesson: (lesson: any) => void;
  onDeleteLesson: (id: string) => void;
  onCancelDeleteLesson: () => void;
  onLinksLesson: (data: { id: string; title: string; links: LessonLink[] }) => void;
  onAddLesson: () => void;
}

<!-- Frontend courses API -->
export async function adminUpdateCourse(id: string, data: Partial<Course>) {
  return client.put(`/api/admin/courses/${id}`, { json: data }).json();
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration + endpoints publish/unpublish para lessons</name>
  <files>
    backend/src/database/migrations/025_lesson_status.sql
    backend/src/routes/admin/courses.ts
  </files>
  <action>
1. **Criar migration 025_lesson_status.sql:**
   - `ALTER TABLE public.lessons ADD COLUMN status text NOT NULL DEFAULT 'draft' CHECK (status IN ('draft', 'published'));`
   - Comentario: "Adiciona status draft/published a aulas. Aulas existentes iniciam como draft."
   - NAO adicionar migration para dados existentes — todas ficam draft por padrao e admin publica manualmente.

2. **Atualizar backend/src/routes/admin/courses.ts:**
   - Adicionar `status` ao `lessonSchema`: `status: z.enum(['draft', 'published']).optional()` — isso permite que PUT /lessons/:id tambem atualize status.
   - Adicionar dois novos endpoints APOS os endpoints de lessons existentes:

   ```
   router.post('/lessons/:id/publish', async (req, res) => {
     const { data, error } = await supabaseAdmin!
       .from('lessons')
       .update({ status: 'published' })
       .eq('id', req.params.id)
       .select()
       .single();
     if (error) return res.status(500).json({ error: error.message });
     res.json({ data });
   });

   router.post('/lessons/:id/unpublish', async (req, res) => {
     const { data, error } = await supabaseAdmin!
       .from('lessons')
       .update({ status: 'draft' })
       .eq('id', req.params.id)
       .select()
       .single();
     if (error) return res.status(500).json({ error: error.message });
     res.json({ data });
   });
   ```

   - Os endpoints sao simples POST sem body. Retornam a lesson atualizada.
   - Estao protegidos pelo middleware requireAdmin ja aplicado no router.
  </action>
  <verify>
    <automated>cd /Users/leonardorodrigues/dev/estrategize-saas/backend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Migration 025 adiciona coluna status em lessons. Endpoints POST /lessons/:id/publish e /unpublish existem e compilam sem erro.</done>
</task>

<task type="auto">
  <name>Task 2: Botao publish/unpublish na lista de aulas do admin</name>
  <files>
    frontend/src/api/courses.ts
    frontend/src/features/admin/components/AdminCursoDetailLessons/LessonPublishButton.tsx
    frontend/src/features/admin/components/AdminCursoDetailLessons/AdminCursoDetailLessons.tsx
    frontend/src/features/admin/components/AdminCursoDetailLessons/index.ts
    frontend/src/features/admin/components/AdminCursoDetailPage/AdminCursoDetailPage.tsx
  </files>
  <action>
1. **Adicionar funcoes na API do frontend** (em `frontend/src/api/courses.ts`):
   - `export async function adminPublishLesson(id: string) { return client.post(`/api/admin/courses/lessons/${id}/publish`).json(); }`
   - `export async function adminUnpublishLesson(id: string) { return client.post(`/api/admin/courses/lessons/${id}/unpublish`).json(); }`

2. **Criar LessonPublishButton.tsx** (novo micro-modulo, max 40 linhas):
   - Props: `{ lessonId: string; status: 'draft' | 'published'; onPublish: (id: string) => void; onUnpublish: (id: string) => void; isPending: boolean }`
   - Se status === 'published': renderizar circulo verde (8x8, bg-green-500 rounded-full inline-block) + texto "Pub" em text-[10px], ao clicar chama onUnpublish(lessonId)
   - Se status === 'draft': renderizar circulo cinza (8x8, bg-[var(--text-tertiary)] rounded-full inline-block) + texto "Draft" em text-[10px], ao clicar chama onPublish(lessonId)
   - Botao wrapper com `disabled={isPending}` e `title` acessivel ("Publicar aula" ou "Despublicar aula")
   - Estilo hover: `hover:opacity-80 transition-opacity`

3. **Atualizar AdminCursoDetailLessons.tsx** (manter max 80 linhas):
   - Adicionar novas props: `onPublishLesson: (id: string) => void; onUnpublishLesson: (id: string) => void; isPublishLessonPending: boolean;`
   - Importar LessonPublishButton
   - No JSX de cada lesson, adicionar `<LessonPublishButton>` entre o numero e o titulo (ou ao lado dos botoes de acao)
   - Passar `status={lesson.status ?? 'draft'}` para compatibilidade com dados existentes sem status
   - O arquivo atual tem 54 linhas. Com a adicao do import (1 linha), props (3 linhas) e o componente no JSX (1 linha) fica em ~59 linhas — dentro do limite de 80.

4. **Atualizar index.ts** barrel para exportar LessonPublishButton tambem.

5. **No aggregator AdminCursoDetailPage** (`frontend/src/features/admin/components/AdminCursoDetailPage/AdminCursoDetailPage.tsx`):
   - Importar `{ adminPublishLesson, adminUnpublishLesson }` de `../../../../api/courses.ts`
   - Adicionar mutations para publish/unpublish lesson usando useMutation + adminPublishLesson/adminUnpublishLesson
   - Invalidar query `['admin-course', courseId]` no onSuccess de ambas mutations
   - Passar os novos callbacks e isPending como props para AdminCursoDetailLessons

Regras:
- LessonPublishButton NAO faz fetch — recebe callbacks via props
- AdminCursoDetailLessons NAO usa useQuery/useMutation — recebe tudo via props
- Toda logica de data fetching fica no aggregator
  </action>
  <verify>
    <automated>cd /Users/leonardorodrigues/dev/estrategize-saas/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Cada aula mostra indicador visual de status (draft/published). Botao publica ou despublica aula com 1 clique. LessonPublishButton e micro-modulo independente com max 40 linhas. AdminCursoDetailLessons permanece abaixo de 80 linhas.</done>
</task>

</tasks>

<verification>
1. TypeScript compila sem erros (backend e frontend)
2. Migration 025 contem ALTER TABLE com coluna status e CHECK constraint
3. Endpoints publish/unpublish existem no router de admin courses
4. LessonPublishButton renderiza estado visual correto para draft e published
5. Nenhum arquivo excede limite de linhas (40 para LessonPublishButton, 80 para AdminCursoDetailLessons)
</verification>

<success_criteria>
- Aulas criadas iniciam como draft por padrao
- Admin publica ou despublica aula individual com 1 clique na pagina de detalhe do curso
- Status da aula e visivel na lista (indicador verde = published, cinza = draft)
- Frontend compila sem erros TypeScript
- Backend compila sem erros TypeScript
- Todos os arquivos dentro dos limites de linhas da arquitetura modular
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-cursos/03-03-SUMMARY.md`
</output>
