---
phase: 03-admin-cursos
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - frontend/src/features/admin/components/AdminCursosPage/AdminCursosPage.tsx
  - frontend/src/features/admin/components/AdminCursosPage/CourseCard.tsx
  - frontend/src/features/admin/components/AdminCursosPage/CourseCreateModal.tsx
  - frontend/src/features/admin/components/AdminCursosPage/CourseStatusTabs.tsx
  - frontend/src/features/admin/components/AdminCursosPage/index.ts
  - frontend/src/features/admin/components/AdminCursoDetailHeader/AdminCursoDetailHeader.tsx
autonomous: true
requirements: [ADMN-02]

must_haves:
  truths:
    - "Admin filtra cursos por status (Todos, Publicados, Rascunhos, Arquivados)"
    - "Admin publica ou despublica curso com 1 clique direto do card na lista"
    - "Thumbnail do curso aparece no card da lista"
    - "Admin associa curso a um plano Stripe via dropdown no modal de criacao e no header do detalhe"
    - "Organizacao visual torna o fluxo intuitivo com contagem por status"
  artifacts:
    - path: "frontend/src/features/admin/components/AdminCursosPage/CourseStatusTabs.tsx"
      provides: "Tabs de filtro por status com contagens"
      min_lines: 20
    - path: "frontend/src/features/admin/components/AdminCursosPage/CourseCard.tsx"
      provides: "Card com thumbnail, badge de status, botoes publish/unpublish"
    - path: "frontend/src/features/admin/components/AdminCursosPage/CourseCreateModal.tsx"
      provides: "Modal com dropdown de plano Stripe"
    - path: "frontend/src/features/admin/components/AdminCursoDetailHeader/AdminCursoDetailHeader.tsx"
      provides: "Header com dropdown de plano e botao publish/unpublish"
  key_links:
    - from: "CourseCreateModal.tsx"
      to: "adminListProducts"
      via: "useQuery para carregar planos no dropdown"
      pattern: "adminListProducts"
    - from: "AdminCursosPage.tsx"
      to: "CourseStatusTabs.tsx"
      via: "statusFilter state + props"
      pattern: "statusFilter"
    - from: "AdminCursoDetailHeader.tsx"
      to: "adminUpdateCourse"
      via: "mutation para salvar stripe_product_id"
      pattern: "stripe_product_id"
---

<objective>
Melhorar UX da pagina AdminCursos com filtro por status, thumbnails nos cards, e dropdown de associacao curso-plano.

Purpose: Tornar o fluxo de publicacao intuitivo — admin encontra, filtra, publica e associa curso a plano sem sair da interface.
Output: AdminCursosPage com tabs de status, cards visuais, e dropdown de plano no modal de criacao e header do detalhe.
</objective>

<execution_context>
@/Users/leonardorodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leonardorodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-admin-cursos/03-01-SUMMARY.md

<interfaces>
<!-- AdminCursosPage aggregator (134 lines) — imports -->
import { adminGetCourses, adminCreateCourse, adminPublishCourse, adminArchiveCourse } from '../../../../api/courses.ts';
import { CourseCard } from './CourseCard.tsx';
import { CourseCreateModal } from './CourseCreateModal.tsx';

<!-- CourseCard props -->
interface CourseCardProps {
  course: { id: string; title: string; status: string; cover_url?: string; modules?: { lessons?: unknown[] }[] };
  onPublish: (id: string) => void;
  onArchive: (id: string) => void;
  isPublishPending: boolean;
  isArchivePending: boolean;
}

<!-- CourseCreateModal props -->
interface CourseCreateModalProps {
  open: boolean;
  form: CourseFormData;
  pendingId: string;
  isPending: boolean;
  onClose: () => void;
  onSubmit: () => void;
  setForm: React.Dispatch<React.SetStateAction<CourseFormData>>;
}

<!-- Frontend stripe API -->
export function adminListProducts() {
  return client.get('/api/admin/stripe/products').json<{ data: StripeProduct[] }>();
}

export interface StripeProduct {
  id: string;
  name: string;
  price_cents: number;
  billing_interval: 'month' | 'year' | 'one_time';
  status: 'active' | 'archived';
}

<!-- AdminCursoDetailHeader (59 lines) -->
export function AdminCursoDetailHeader({ course, onEditClick }: {
  course: Course;
  onEditClick: () => void;
})

<!-- Frontend courses API -->
export async function adminUpdateCourse(id: string, data: Partial<Course>) {
  return client.put(`/api/admin/courses/${id}`, { json: data }).json();
}

<!-- Existing UI components -->
import { Badge } from '../../../../components/ui/Badge.tsx';
import { Button } from '../../../../components/ui/Button.tsx';
import { Modal } from '../../../../components/ui/Modal.tsx';
import { Input } from '../../../../components/ui/Input.tsx';
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CourseStatusTabs + melhorar CourseCard + atualizar aggregator</name>
  <files>
    frontend/src/features/admin/components/AdminCursosPage/CourseStatusTabs.tsx
    frontend/src/features/admin/components/AdminCursosPage/CourseCard.tsx
    frontend/src/features/admin/components/AdminCursosPage/AdminCursosPage.tsx
    frontend/src/features/admin/components/AdminCursosPage/index.ts
  </files>
  <action>
1. **Criar CourseStatusTabs.tsx** (novo micro-modulo, max 60 linhas):
   - Props: `{ activeStatus: string; onStatusChange: (s: string) => void; counts: Record<string, number> }`
   - Renderiza tabs: "Todos", "Publicados", "Rascunhos", "Arquivados"
   - Cada tab mostra contagem entre parenteses: "Publicados (3)"
   - Tab ativa tem estilo `border-b-2 border-[var(--text-primary)] text-[var(--text-primary)]`
   - Tabs inativas: `text-[var(--text-tertiary)] hover:text-[var(--text-secondary)]`
   - Layout: flex row com gap-4, border-b no container

2. **Melhorar CourseCard.tsx** (atualizar, manter max 80 linhas):
   - Adicionar ao tipo do course: `stripe_products?: { id: string; name: string; price_cents: number; billing_interval: string } | null`
   - Se curso tem plano associado, mostrar badge pequeno com nome do plano abaixo da contagem de modulos/aulas
   - Formato: `<span className="text-xs text-[var(--text-muted)]">Plano: {course.stripe_products?.name}</span>`
   - Manter thumbnails existentes (cover_url ja renderizado como img 40x40)
   - Melhorar botoes de acao: usar icone de toggle (circulo verde para publicar, circulo cinza para arquivar) ao lado do texto
   - Adicionar `title` attribute nos botoes para acessibilidade

3. **Atualizar AdminCursosPage.tsx** (aggregator, manter max 200 linhas):
   - Adicionar estado: `const [statusFilter, setStatusFilter] = useState('all')`
   - Calcular contagens: `{ all: courses.length, published: ..., draft: ..., archived: ... }`
   - Filtrar cursos pelo statusFilter antes de renderizar (se 'all', mostrar todos)
   - Importar e renderizar `<CourseStatusTabs>` entre o header e a lista
   - Adicionar `stripe_product_id` ao CourseFormData: `stripe_product_id?: string | null`
   - Passar stripe_product_id no handleCreate

4. **Atualizar index.ts** barrel para exportar CourseStatusTabs tambem (se necessario para testes).
  </action>
  <verify>
    <automated>cd /Users/leonardorodrigues/dev/estrategize-saas/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>AdminCursosPage mostra tabs de filtro por status com contagens. CourseCard exibe badge de plano associado. Filtro funciona corretamente.</done>
</task>

<task type="auto">
  <name>Task 2: Dropdown de plano no CourseCreateModal e no AdminCursoDetailHeader</name>
  <files>
    frontend/src/features/admin/components/AdminCursosPage/CourseCreateModal.tsx
    frontend/src/features/admin/components/AdminCursoDetailHeader/AdminCursoDetailHeader.tsx
  </files>
  <action>
1. **Atualizar CourseCreateModal.tsx** (manter max 80 linhas):
   - Adicionar `stripe_product_id` ao CourseFormData interface (string | null | undefined)
   - Importar `{ adminListProducts }` de `../../../../api/stripe.ts`
   - Importar `{ useQuery }` de `@tanstack/react-query`
   - Fazer `useQuery({ queryKey: ['admin-stripe-products'], queryFn: adminListProducts })` para carregar planos
   - Adicionar um `<select>` estilizado (mesma classe dos inputs existentes) abaixo da descricao e acima do upload:
     - Label: "Plano associado"
     - Opcao default: "Nenhum (acesso livre)" com value=""
     - Opcoes: cada stripe_product ativo com `value={product.id}` e text `{product.name} - R$ {(product.price_cents/100).toFixed(2)}/{interval}`
   - onChange: `setForm(f => ({ ...f, stripe_product_id: e.target.value || null }))`
   - O select NAO e obrigatorio (curso pode ser criado sem plano)
   - Estilo do select: `w-full rounded-[var(--radius-sm)] border border-[var(--border-hairline)] bg-[var(--bg-surface-1)] px-3 py-2 text-sm text-[var(--text-primary)]`

2. **Atualizar AdminCursoDetailHeader.tsx** (manter max 80 linhas):
   - Importar `{ useQuery, useMutation, useQueryClient }` de `@tanstack/react-query`
   - Importar `{ adminListProducts }` de `../../../../api/stripe.ts`
   - Importar `{ adminUpdateCourse }` de `../../../../api/courses.ts`
   - Carregar planos via useQuery
   - Adicionar um `<select>` inline no header (ao lado do titulo ou abaixo) mostrando plano atual do curso
   - Value: `course.stripe_product_id ?? ""`
   - onChange: criar mutation que faz `adminUpdateCourse(course.id, { stripe_product_id: value || null })` e invalida query `['admin-course', course.id]`
   - Mostrar badge do plano associado se existir, ou "Sem plano" em texto muted
   - Adicionar botao "Publicar"/"Despublicar" no header (importar adminPublishCourse/adminArchiveCourse)
     - Se draft/archived: mostrar botao "Publicar" (verde)
     - Se published: mostrar botao "Despublicar" (ghost)
     - onClick chama a mutation correspondente e invalida queries

Regras:
- NAO usar Context API — dados vem de useQuery local
- NAO exceder 80 linhas por arquivo
- Se CourseCreateModal ficar acima de 80 linhas, extrair o select para um micro-modulo `PlanSelect.tsx` na mesma pasta
  </action>
  <verify>
    <automated>cd /Users/leonardorodrigues/dev/estrategize-saas/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Dropdown de plano funciona no modal de criacao (carrega planos ativos do Stripe) e no header do detalhe (salva associacao via PUT). Botao publicar/despublicar presente no header do detalhe.</done>
</task>

</tasks>

<verification>
1. TypeScript compila sem erros
2. CourseStatusTabs renderiza com contagens corretas
3. Filtro por status funciona (selecionar "Publicados" mostra apenas published)
4. CourseCreateModal tem select de plano carregando dados reais
5. AdminCursoDetailHeader tem select de plano e botao publish/unpublish
6. Nenhum arquivo excede limite de linhas (80 para micro-modulos, 200 para aggregator)
</verification>

<success_criteria>
- Admin filtra cursos por status via tabs com contagens
- Card mostra thumbnail e badge de plano quando associado
- Modal de criacao permite selecionar plano Stripe via dropdown
- Header do detalhe permite mudar plano e publicar/despublicar com 1 clique
- Todos os arquivos dentro dos limites de linhas da arquitetura modular
- Frontend compila sem erros TypeScript
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-cursos/03-02-SUMMARY.md`
</output>
