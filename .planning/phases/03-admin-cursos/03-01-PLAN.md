---
phase: 03-admin-cursos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/database/migrations/024_course_plan_link.sql
  - backend/src/routes/admin/courses.ts
autonomous: true
requirements: [ADMN-02]

must_haves:
  truths:
    - "Backend aceita associar um curso a um stripe_product via PATCH"
    - "GET /api/admin/courses retorna stripe_product_id de cada curso"
    - "GET /api/admin/courses retorna stripe_product name quando associado"
  artifacts:
    - path: "backend/src/database/migrations/024_course_plan_link.sql"
      provides: "Coluna stripe_product_id na tabela courses"
      contains: "stripe_product_id"
    - path: "backend/src/routes/admin/courses.ts"
      provides: "Endpoint PATCH /:id com stripe_product_id + GET retornando join"
  key_links:
    - from: "backend/src/routes/admin/courses.ts"
      to: "courses.stripe_product_id"
      via: "supabaseAdmin update/select"
      pattern: "stripe_product_id"
---

<objective>
Adicionar coluna stripe_product_id na tabela courses e atualizar o backend para suportar associacao curso-plano.

Purpose: Permitir que o admin associe um curso a um plano Stripe via interface (pre-requisito para o dropdown no frontend).
Output: Migration 024 aplicavel + endpoints GET/PATCH retornando dados de plano associado.
</objective>

<execution_context>
@/Users/leonardorodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leonardorodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Tabela stripe_products (migration 023) -->
CREATE TABLE public.stripe_products (
  id               uuid        DEFAULT gen_random_uuid() PRIMARY KEY,
  name             text        NOT NULL,
  stripe_product_id text       UNIQUE,
  stripe_price_id  text        UNIQUE,
  price_cents      integer     NOT NULL,
  credits          integer     NOT NULL DEFAULT 0,
  billing_interval text        NOT NULL,
  status           text        NOT NULL DEFAULT 'active',
  created_at       timestamptz DEFAULT now(),
  updated_at       timestamptz DEFAULT now()
);

<!-- Tabela courses (migration 006) -->
CREATE TABLE public.courses (
  id           uuid PRIMARY KEY DEFAULT uuid_generate_v4(),
  title        text NOT NULL,
  description  text,
  cover_url    text,
  status       text NOT NULL DEFAULT 'draft',
  sort_order   integer NOT NULL DEFAULT 0,
  created_at   timestamptz NOT NULL DEFAULT now(),
  updated_at   timestamptz NOT NULL DEFAULT now()
);

<!-- Backend courses.ts existing courseSchema -->
const courseSchema = z.object({
  title: z.string().min(1).max(200),
  description: z.string().optional(),
  cover_url: z.string().url().optional().nullable(),
  banner_url: z.string().url().optional().nullable(),
  status: z.enum(['draft', 'published', 'archived']).optional(),
  sort_order: z.number().int().min(0).optional(),
});

<!-- GET / existing select -->
.select(`id, title, status, cover_url, sort_order, created_at, modules (id, lessons (id))`)

<!-- Frontend API (stripe.ts) -->
export function adminListProducts() {
  return client.get('/api/admin/stripe/products').json<{ data: StripeProduct[] }>();
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 024 — adicionar stripe_product_id em courses</name>
  <files>backend/src/database/migrations/024_course_plan_link.sql</files>
  <action>
Criar migration que adiciona coluna `stripe_product_id uuid REFERENCES public.stripe_products(id) ON DELETE SET NULL` na tabela `courses`. A coluna e nullable (curso pode nao ter plano associado).

Conteudo do arquivo SQL:
1. ALTER TABLE public.courses ADD COLUMN stripe_product_id uuid REFERENCES public.stripe_products(id) ON DELETE SET NULL;
2. CREATE INDEX idx_courses_stripe_product_id ON public.courses(stripe_product_id);
3. Comentario explicando que este link permite associar um curso a um plano Stripe para controle de acesso.

NAO criar junction table — relacao e 1:N (um stripe_product pode dar acesso a N cursos, mas cada curso tem no maximo 1 plano associado).
  </action>
  <verify>
    <automated>cat backend/src/database/migrations/024_course_plan_link.sql | grep -c "stripe_product_id"</automated>
  </verify>
  <done>Migration file existe com ALTER TABLE adicionando stripe_product_id como FK nullable para stripe_products.</done>
</task>

<task type="auto">
  <name>Task 2: Backend — atualizar courseSchema e GET para incluir plano</name>
  <files>backend/src/routes/admin/courses.ts</files>
  <action>
Modificar `backend/src/routes/admin/courses.ts`:

1. Adicionar `stripe_product_id` ao Zod `courseSchema`: `stripe_product_id: z.string().uuid().optional().nullable()`

2. Atualizar o GET `/` (lista de cursos) para incluir join com stripe_products:
   - Mudar o select de:
     `id, title, status, cover_url, sort_order, created_at, modules (id, lessons (id))`
   - Para:
     `id, title, status, cover_url, sort_order, created_at, stripe_product_id, stripe_products (id, name, price_cents, billing_interval), modules (id, lessons (id))`
   - Isso usa o Supabase foreign key join automatico para retornar dados do plano associado.

3. Atualizar o GET `/:id` (detalhe) para tambem incluir stripe_products no select:
   - Adicionar `stripe_product_id, stripe_products (id, name, price_cents, billing_interval)` ao select existente.

4. O PUT `/:id` ja aceita campos do courseSchema, entao o stripe_product_id sera aceito automaticamente apos adicionado ao schema.

NAO criar rota nova — o PUT existente ja cobre a atualizacao. O frontend enviara `{ stripe_product_id: "uuid" }` via PUT.
  </action>
  <verify>
    <automated>cd /Users/leonardorodrigues/dev/estrategize-saas/backend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>GET /api/admin/courses retorna stripe_product_id e dados do plano associado. PUT aceita stripe_product_id para associacao.</done>
</task>

</tasks>

<verification>
1. Migration file contem ALTER TABLE com FK para stripe_products
2. TypeScript compila sem erros
3. GET endpoint select inclui stripe_products join
4. courseSchema inclui stripe_product_id
</verification>

<success_criteria>
- Migration 024 cria coluna stripe_product_id nullable em courses
- GET /api/admin/courses retorna stripe_product_id e nome do plano quando associado
- PUT /api/admin/courses/:id aceita stripe_product_id para associar curso a plano
- Backend compila sem erros TypeScript
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-cursos/03-01-SUMMARY.md`
</output>
