---
phase: 18-wizard-de-criacao
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/database/migrations/029_consultancies_extra_fields.sql
  - backend/src/routes/consultancies.ts
  - frontend/src/api/consultancies.ts
  - frontend/src/features/consultorias/components/CreateConsultancyWizard/wizard.types.ts
autonomous: true
requirements:
  - CONS-03

must_haves:
  truths:
    - "POST /api/consultancies aceita ticket, has_team, has_website e current_stage sem retornar 400"
    - "PUT /api/consultancies/:id aceita os mesmos campos novos"
    - "Interface Consultancy no frontend inclui ticket, has_team, has_website e current_stage"
    - "CreateConsultancyPayload inclui start_date, ticket, has_team, has_website e current_stage"
    - "WizardState inclui ticket, start_date, has_team, has_website e current_stage com valores iniciais corretos"
  artifacts:
    - path: "backend/src/database/migrations/029_consultancies_extra_fields.sql"
      provides: "ADD COLUMN para ticket, has_team, has_website, current_stage na tabela consultancies"
      contains: "ALTER TABLE consultancies ADD COLUMN"
    - path: "backend/src/routes/consultancies.ts"
      provides: "createSchema e updateSchema com os 4 campos novos"
      exports: ["createSchema", "updateSchema"]
    - path: "frontend/src/api/consultancies.ts"
      provides: "Consultancy interface + CreateConsultancyPayload atualizados"
    - path: "frontend/src/features/consultorias/components/CreateConsultancyWizard/wizard.types.ts"
      provides: "WizardState + WIZARD_INITIAL atualizados"
  key_links:
    - from: "backend/src/routes/consultancies.ts"
      to: "backend/src/database/migrations/029_consultancies_extra_fields.sql"
      via: "Zod schema valida campos que migration adiciona ao banco"
      pattern: "ticket.*z\\.number|has_team.*z\\.boolean"
    - from: "frontend/src/features/consultorias/components/CreateConsultancyWizard/wizard.types.ts"
      to: "frontend/src/api/consultancies.ts"
      via: "WizardState campos mapeiam 1:1 para CreateConsultancyPayload"
      pattern: "WizardState|CreateConsultancyPayload"
---

<objective>
Adicionar os 4 novos campos (ticket, has_team, has_website, current_stage) ao banco de dados e atualizar os contratos de dados (Zod schemas no backend, interfaces TypeScript no frontend e WizardState).

Purpose: Sem esses campos na camada de dados, o wizard não consegue persistir as informações coletadas nos Steps. Este plano estabelece os contratos que o plano 18-02 implementará na UI.

Output: Migration SQL, Zod schemas atualizados, Consultancy interface atualizada, CreateConsultancyPayload atualizado, WizardState atualizado.
</objective>

<execution_context>
@/Users/leonardorodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leonardorodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 029 — ADD COLUMN nos campos extras de consultoria</name>
  <files>backend/src/database/migrations/029_consultancies_extra_fields.sql</files>
  <action>
Criar migration SQL que adiciona 4 colunas à tabela `consultancies` existente:

```sql
ALTER TABLE consultancies
  ADD COLUMN IF NOT EXISTS ticket INTEGER,
  ADD COLUMN IF NOT EXISTS has_team BOOLEAN NOT NULL DEFAULT FALSE,
  ADD COLUMN IF NOT EXISTS has_website BOOLEAN NOT NULL DEFAULT FALSE,
  ADD COLUMN IF NOT EXISTS current_stage TEXT;
```

Incluir comentário de rollback ao final do arquivo:

```sql
-- Rollback: ALTER TABLE consultancies DROP COLUMN IF EXISTS ticket, DROP COLUMN IF EXISTS has_team, DROP COLUMN IF EXISTS has_website, DROP COLUMN IF EXISTS current_stage;
```

Nenhuma outra tabela ou índice necessário. has_team e has_website têm DEFAULT FALSE (não nullable). ticket e current_stage são nullable.

Executar a migration no Supabase via Dashboard SQL Editor ou CLI antes de executar o plano 18-02.
  </action>
  <verify>
    <automated>grep -c "ADD COLUMN" /Users/leonardorodrigues/dev/estrategize-saas/backend/src/database/migrations/029_consultancies_extra_fields.sql</automated>
  </verify>
  <done>Arquivo existe com 4 ADD COLUMN statements para ticket, has_team, has_website e current_stage</done>
</task>

<task type="auto">
  <name>Task 2: Backend Zod schemas + Frontend types + WizardState</name>
  <files>
    backend/src/routes/consultancies.ts,
    frontend/src/api/consultancies.ts,
    frontend/src/features/consultorias/components/CreateConsultancyWizard/wizard.types.ts
  </files>
  <action>
**backend/src/routes/consultancies.ts** — atualizar `createSchema` e `updateSchema`:

No `createSchema` (linha ~65), adicionar após `template`:
```typescript
ticket: z.number().int().min(0).optional(),
has_team: z.boolean().optional(),
has_website: z.boolean().optional(),
current_stage: z.string().max(255).optional(),
```

No `updateSchema` (linha ~77), adicionar após `template`:
```typescript
ticket: z.number().int().min(0).nullable().optional(),
has_team: z.boolean().optional(),
has_website: z.boolean().optional(),
current_stage: z.string().max(255).nullable().optional(),
```

`start_date` já está nos dois schemas — não alterar.

---

**frontend/src/api/consultancies.ts** — atualizar interface `Consultancy` (linha ~27). Adicionar após `priority`:
```typescript
ticket: number | null;
has_team: boolean;
has_website: boolean;
current_stage: string | null;
```

Atualizar `CreateConsultancyPayload` (linha ~79). Adicionar após `priority`:
```typescript
start_date?: string;
ticket?: number;
has_team?: boolean;
has_website?: boolean;
current_stage?: string;
```

Nota: `start_date` já existe na tabela consultancies e no `createSchema` do backend mas não estava no `CreateConsultancyPayload` — adicionar agora.

---

**frontend/src/features/consultorias/components/CreateConsultancyWizard/wizard.types.ts** — reescrever completamente:

```typescript
import type { ConsultancyTemplate } from '../../services/consultorias.api.ts';

export interface WizardState {
  template: ConsultancyTemplate | null;
  title: string;
  client_name: string;
  niche: string;
  instagram: string;
  ticket: string;        // user digita número como string; wizard converte antes de enviar
  start_date: string;    // formato ISO date string 'YYYY-MM-DD' ou vazio
  problem: string;
  tried: string;
  goal90: string;
  current_stage: string;
  has_team: boolean;
  has_website: boolean;
}

export const WIZARD_INITIAL: WizardState = {
  template: null,
  title: '',
  client_name: '',
  niche: '',
  instagram: '',
  ticket: '',
  start_date: '',
  problem: '',
  tried: '',
  goal90: '',
  current_stage: '',
  has_team: false,
  has_website: false,
};
```

Manter a importação de `ConsultancyTemplate`. Arquivo deve ter no máximo 30 linhas.
  </action>
  <verify>
    <automated>cd /Users/leonardorodrigues/dev/estrategize-saas/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compila sem erros. WizardState tem 13 campos. createSchema e updateSchema no backend aceitam os 4 novos campos. Consultancy interface e CreateConsultancyPayload refletem os campos novos.</done>
</task>

</tasks>

<verification>
1. `backend/src/database/migrations/029_consultancies_extra_fields.sql` existe com 4 ADD COLUMN
2. `npx tsc --noEmit` no frontend sem erros relacionados a wizard.types.ts ou consultancies.ts
3. `grep "ticket" backend/src/routes/consultancies.ts` retorna linhas em createSchema e updateSchema
4. `grep "has_team\|has_website\|current_stage" frontend/src/api/consultancies.ts` retorna resultados na interface Consultancy e CreateConsultancyPayload
</verification>

<success_criteria>
- Migration 029 criada e pronta para execução no Supabase
- Backend aceita os 4 novos campos sem retornar 400 (Zod valida corretamente)
- Frontend compila sem erros de TypeScript
- WizardState tem os novos campos com valores iniciais adequados
- CreateConsultancyPayload inclui start_date, ticket, has_team, has_website, current_stage
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/18-wizard-de-criacao/18-01-SUMMARY.md`
</output>
