---
phase: 18-wizard-de-criacao
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - frontend/src/features/consultorias/components/CreateConsultancyWizard/steps/StepBasicData.tsx
  - frontend/src/features/consultorias/components/CreateConsultancyWizard/steps/StepContext.tsx
  - frontend/src/features/consultorias/components/CreateConsultancyWizard/CreateConsultancyWizard.tsx
autonomous: true
requirements:
  - CONS-03

must_haves:
  truths:
    - "StepBasicData exibe campo ticket (número, opcional) e start_date (data, opcional)"
    - "StepContext exibe current_stage (textarea), has_team (checkbox) e has_website (checkbox), mantendo problem e goal90"
    - "CreateConsultancyWizard envia ticket como integer, start_date, current_stage, has_team e has_website na mutation"
    - "Consultoria criada persiste phase='onboarding' e os novos campos populados"
    - "Fluxo 4 etapas (Template → BasicData → Context → Generating) permanece íntegro"
  artifacts:
    - path: "frontend/src/features/consultorias/components/CreateConsultancyWizard/steps/StepBasicData.tsx"
      provides: "Campos ticket + start_date adicionados"
      min_lines: 30
    - path: "frontend/src/features/consultorias/components/CreateConsultancyWizard/steps/StepContext.tsx"
      provides: "current_stage textarea + has_team checkbox + has_website checkbox"
      min_lines: 40
    - path: "frontend/src/features/consultorias/components/CreateConsultancyWizard/CreateConsultancyWizard.tsx"
      provides: "Mutation atualizada com os 5 novos campos"
  key_links:
    - from: "frontend/src/features/consultorias/components/CreateConsultancyWizard/CreateConsultancyWizard.tsx"
      to: "frontend/src/api/consultancies.ts"
      via: "createConsultancy(payload) incluindo ticket, start_date, has_team, has_website, current_stage, strategic_summary, real_bottleneck"
      pattern: "form\\.ticket|form\\.start_date|form\\.has_team|form\\.has_website|form\\.current_stage|form\\.goal90|form\\.problem"
    - from: "frontend/src/features/consultorias/components/CreateConsultancyWizard/steps/StepBasicData.tsx"
      to: "frontend/src/features/consultorias/components/CreateConsultancyWizard/wizard.types.ts"
      via: "form.ticket e form.start_date lidos do WizardState"
      pattern: "form\\.ticket|form\\.start_date"
---

<objective>
Atualizar os componentes de UI do wizard para coletar os campos adicionados no plano 18-01, e atualizar a mutation do aggregator para enviar todos os novos campos ao backend.

Purpose: Com os contratos de dados estabelecidos (18-01), este plano implementa a UI que coleta as informações e o wiring que as persiste.

Output: StepBasicData com ticket + start_date, StepContext com current_stage + checkboxes, CreateConsultancyWizard com mutation completa.
</objective>

<execution_context>
@/Users/leonardorodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leonardorodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-wizard-de-criacao/18-01-SUMMARY.md

@frontend/src/features/consultorias/components/CreateConsultancyWizard/wizard.types.ts
@frontend/src/features/consultorias/components/CreateConsultancyWizard/CreateConsultancyWizard.tsx
@frontend/src/features/consultorias/components/CreateConsultancyWizard/steps/StepBasicData.tsx
@frontend/src/features/consultorias/components/CreateConsultancyWizard/steps/StepContext.tsx

<interfaces>
<!-- Contratos estabelecidos no plano 18-01. Executor usa diretamente. -->

From wizard.types.ts (após 18-01):
```typescript
export interface WizardState {
  template: ConsultancyTemplate | null;
  title: string;
  client_name: string;
  niche: string;
  instagram: string;
  ticket: string;       // string — user digita; wizard converte para int antes de enviar
  start_date: string;   // 'YYYY-MM-DD' ou ''
  problem: string;
  tried: string;
  goal90: string;
  current_stage: string;
  has_team: boolean;
  has_website: boolean;
}
```

From frontend/src/api/consultancies.ts (após 18-01):
```typescript
export interface CreateConsultancyPayload {
  title: string;
  client_name?: string;
  phase?: ConsultancyPhase;
  niche?: string;
  instagram?: string;
  template?: ConsultancyTemplate;
  priority?: ConsultancyPriority;
  start_date?: string;
  ticket?: number;
  has_team?: boolean;
  has_website?: boolean;
  current_stage?: string;
  strategic_summary?: string;
  real_bottleneck?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StepBasicData — adicionar ticket e start_date</name>
  <files>frontend/src/features/consultorias/components/CreateConsultancyWizard/steps/StepBasicData.tsx</files>
  <action>
Reescrever `StepBasicData.tsx` adicionando 2 campos novos após o campo `instagram`. Manter os 4 campos existentes intactos (title, client_name, niche, instagram). Limite: 80 linhas.

**Campos a adicionar:**

1. `ticket` — input numérico, label "Ticket médio (R$)", placeholder "Ex: 3000", opcional. Renderizar como `<Input>` com `type="number"` e `min="0"`. onChange: `onFormChange((f) => ({ ...f, ticket: e.target.value }))`.

2. `start_date` — input de data, label "Data de início (opcional)", tipo `date`. Renderizar como `<Input>` com `type="date"`. onChange: `onFormChange((f) => ({ ...f, start_date: e.target.value }))`.

Ambos os campos são opcionais — não bloquear o avanço se vazios. O componente já valida apenas `title` (responsabilidade do pai via `titleError`).

Estrutura final do formulário em StepBasicData:
- title (obrigatório, com titleError)
- client_name (opcional)
- niche (opcional)
- instagram (opcional)
- ticket (opcional, number)
- start_date (opcional, date)

Manter exatamente o mesmo padrão visual e de motion que os campos existentes. Nenhum estado local — tudo via `form` + `onFormChange`.
  </action>
  <verify>
    <automated>cd /Users/leonardorodrigues/dev/estrategize-saas/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>StepBasicData renderiza 6 campos (4 existentes + ticket + start_date). Sem erros de TypeScript. Arquivo ≤80 linhas.</done>
</task>

<task type="auto">
  <name>Task 2: StepContext — adicionar current_stage + checkboxes + CreateConsultancyWizard mutation</name>
  <files>
    frontend/src/features/consultorias/components/CreateConsultancyWizard/steps/StepContext.tsx,
    frontend/src/features/consultorias/components/CreateConsultancyWizard/CreateConsultancyWizard.tsx
  </files>
  <action>
**StepContext.tsx** — reescrever para incluir os novos campos mantendo os existentes. Limite: 80 linhas.

Ordem dos campos na UI:
1. "Dores relatadas" (era "Qual o principal problema do cliente?") — textarea 3 rows, `form.problem`
2. "Objetivo principal" (era "Qual o objetivo em 90 dias?") — textarea 2 rows, `form.goal90`
3. "O que ele já tentou?" — textarea 2 rows, `form.tried` (mantém label igual)
4. "Estágio atual do negócio" — textarea 2 rows, `form.current_stage`, placeholder "Ex: fatura R$10k/mês, time de 2 pessoas, sem processo definido…"
5. Dois checkboxes lado a lado (grid 2 colunas):
   - "Tem equipe?" → `form.has_team`
   - "Tem site ou loja online?" → `form.has_website`

**Padrão dos checkboxes** — usar elemento `<label>` clicável com `<input type="checkbox">` inline, estilo consistente com o design system existente (texto `text-[13px] text-[var(--text-secondary)]`):
```tsx
<label className="flex items-center gap-2 cursor-pointer text-[13px] text-[var(--text-secondary)]">
  <input
    type="checkbox"
    checked={form.has_team}
    onChange={(e) => onFormChange((f) => ({ ...f, has_team: e.target.checked }))}
    className="rounded border-[var(--border-default)] accent-[var(--accent)]"
  />
  Tem equipe?
</label>
```

Manter `textareaClass` como constante de módulo (igual ao arquivo original). Manter AnimatePresence para `mutError`. Manter botões "← Voltar" e "Gerar com IA →".

---

**CreateConsultancyWizard.tsx** — atualizar somente o bloco da mutation (dentro do `setTimeout`, ~linha 48-56). Substituir o objeto de payload para incluir todos os novos campos, incluindo `goal90` → `strategic_summary` e `problem` → `real_bottleneck`:

```typescript
createMutation.mutate({
  title: form.title.trim(),
  ...(form.client_name.trim() ? { client_name: form.client_name.trim() } : {}),
  ...(form.niche.trim() ? { niche: form.niche.trim() } : {}),
  ...(form.instagram.trim() ? { instagram: form.instagram.trim() } : {}),
  ...(form.template ? { template: form.template } : {}),
  ...(form.ticket ? { ticket: parseInt(form.ticket, 10) } : {}),
  ...(form.start_date ? { start_date: form.start_date } : {}),
  ...(form.goal90.trim() ? { strategic_summary: form.goal90.trim() } : {}),
  ...(form.problem.trim() ? { real_bottleneck: form.problem.trim() } : {}),
  ...(form.current_stage.trim() ? { current_stage: form.current_stage.trim() } : {}),
  has_team: form.has_team,
  has_website: form.has_website,
  phase: 'onboarding',
});
```

Nota: `strategic_summary` e `real_bottleneck` devem existir na tabela consultancies (campos de texto). Se não existirem, adicionar à migration 029 antes de executar:
```sql
ALTER TABLE consultancies
  ADD COLUMN IF NOT EXISTS strategic_summary TEXT,
  ADD COLUMN IF NOT EXISTS real_bottleneck TEXT;
```
E incluir no `updateSchema` do backend:
```typescript
strategic_summary: z.string().nullable().optional(),
real_bottleneck: z.string().nullable().optional(),
```

O aggregator permanece ≤200 linhas. Nenhuma outra mudança em CreateConsultancyWizard.tsx.
  </action>
  <verify>
    <automated>cd /Users/leonardorodrigues/dev/estrategize-saas/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>StepContext renderiza 4 textareas + 2 checkboxes. CreateConsultancyWizard envia os 7 novos campos na mutation incluindo strategic_summary populado de form.goal90 e real_bottleneck populado de form.problem quando não-vazios. TypeScript sem erros. Ambos os arquivos dentro dos limites de linhas. strategic_summary e real_bottleneck populados de form.goal90 e form.problem quando não-vazios.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` no frontend passa sem erros
2. StepBasicData.tsx ≤80 linhas com campos ticket e start_date visíveis
3. StepContext.tsx ≤80 linhas com current_stage textarea + 2 checkboxes has_team e has_website
4. CreateConsultancyWizard.tsx ≤200 linhas; grep "has_team\|has_website\|current_stage\|start_date\|parseInt\|strategic_summary\|real_bottleneck" confirma campos no payload
5. Fluxo completo: Template (step 0) → BasicData (step 1) → Context (step 2) → Generating (step 3) funciona sem erro de runtime
</verification>

<success_criteria>
- Etapa 1 (BasicData) coleta: título, nome, nicho, instagram, ticket, data início
- Etapa 2 (Context) coleta: dores relatadas, objetivo principal, o que tentou, estágio atual, tem equipe, tem site
- Wizard cria consultoria com phase='onboarding' e todos os campos novos persistidos
- goal90 persiste como strategic_summary, problem persiste como real_bottleneck
- Frontend compila sem erros de TypeScript
- Nenhum componente excede seu limite de linhas (StepBasicData ≤80L, StepContext ≤80L, Aggregator ≤200L)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/18-wizard-de-criacao/18-02-SUMMARY.md`
</output>
