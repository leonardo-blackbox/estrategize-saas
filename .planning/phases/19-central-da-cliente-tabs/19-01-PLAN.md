---
phase: 19-central-da-cliente-tabs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/consultorias/consultorias.detail.types.ts
  - frontend/src/features/consultorias/components/ConsultoriaDetailPage/ConsultoriaDetailPage.tsx
  - frontend/src/features/consultorias/components/ConsultoriaDetailMemory/ConsultoriaDetailMemory.tsx
autonomous: true
requirements: [CONS-04]

must_haves:
  truths:
    - "Tabs aparecem na ordem: Overview, Chat IA, Reunioes, Documentos, Diagnostico, Action Items, Entregas, Memoria IA, Dados"
    - "Tabs jornada, mercado, conteudo, financeiro nao existem mais"
    - "Aba Memoria IA renderiza o componente ConsultoriaDetailMemory em layout full-width"
    - "Navegacao entre tabs e fluida e nao perde estado"
  artifacts:
    - path: "frontend/src/features/consultorias/consultorias.detail.types.ts"
      provides: "TabKey union type and TABS array with 9 tabs in correct order"
      contains: "overview.*ai.*meetings.*documentos.*diagnosis.*actions.*deliverables.*memory.*dados"
    - path: "frontend/src/features/consultorias/components/ConsultoriaDetailPage/ConsultoriaDetailPage.tsx"
      provides: "Tab content routing for all 9 tabs including memory"
      contains: "ConsultoriaDetailMemory"
  key_links:
    - from: "consultorias.detail.types.ts"
      to: "ConsultoriaDetailTabs.tsx"
      via: "TABS array import"
      pattern: "import.*TABS.*from.*consultorias\\.detail\\.types"
    - from: "ConsultoriaDetailPage.tsx"
      to: "ConsultoriaDetailMemory"
      via: "tab routing conditional"
      pattern: "activeTab === 'memory'"
---

<objective>
Reorganizar as tabs da Central da Cliente na ordem correta, remover tabs nao usadas (jornada, mercado, conteudo, financeiro) e adicionar tab "Memoria IA".

Purpose: A consultora encontra qualquer informacao em menos de 2 cliques com tabs na ordem logica de uso.
Output: TABS reordenadas, routing atualizado, ConsultoriaDetailMemory adaptado para tab.
</objective>

<execution_context>
@/Users/leonardorodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leonardorodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend/src/features/consultorias/consultorias.detail.types.ts
@frontend/src/features/consultorias/components/ConsultoriaDetailPage/ConsultoriaDetailPage.tsx
@frontend/src/features/consultorias/components/ConsultoriaDetailTabs/ConsultoriaDetailTabs.tsx
@frontend/src/features/consultorias/components/ConsultoriaDetailMemory/ConsultoriaDetailMemory.tsx

<interfaces>
<!-- Current TabKey and TABS from consultorias.detail.types.ts -->
From frontend/src/features/consultorias/consultorias.detail.types.ts:
```typescript
export type TabKey =
  | 'overview' | 'dados' | 'diagnosis' | 'jornada'
  | 'meetings' | 'actions' | 'deliverables' | 'ai'
  | 'documentos' | 'mercado' | 'conteudo' | 'financeiro';

export const TABS: TabDef[] = [
  { key: 'overview',     label: 'Visao Geral' },
  { key: 'dados',        label: 'Dados' },
  // ... 12 tabs total
];
```

From ConsultoriaDetailMemory.tsx:
```typescript
interface ConsultoriaDetailMemoryProps { consultancyId: string; }
export function ConsultoriaDetailMemory({ consultancyId }: ConsultoriaDetailMemoryProps)
// Currently styled as sidebar (lg:w-80 shrink-0)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorder TabKey type, TABS array, and update page routing</name>
  <files>
    frontend/src/features/consultorias/consultorias.detail.types.ts,
    frontend/src/features/consultorias/components/ConsultoriaDetailPage/ConsultoriaDetailPage.tsx
  </files>
  <read_first>
    frontend/src/features/consultorias/consultorias.detail.types.ts,
    frontend/src/features/consultorias/components/ConsultoriaDetailPage/ConsultoriaDetailPage.tsx,
    frontend/src/features/consultorias/components/ConsultoriaDetailTabs/ConsultoriaDetailTabs.tsx,
    frontend/src/features/consultorias/hooks/useConsultoriaDetail.ts
  </read_first>
  <action>
    1. In `consultorias.detail.types.ts`:
       - Replace the TabKey union type with exactly these 9 keys in this order:
         `'overview' | 'ai' | 'meetings' | 'documentos' | 'diagnosis' | 'actions' | 'deliverables' | 'memory' | 'dados'`
       - Replace the TABS array with exactly these 9 entries in this order:
         ```
         { key: 'overview',     label: 'Visao Geral' },
         { key: 'ai',           label: 'Chat IA' },
         { key: 'meetings',     label: 'Reunioes' },
         { key: 'documentos',   label: 'Documentos' },
         { key: 'diagnosis',    label: 'Diagnostico' },
         { key: 'actions',      label: 'Plano de Acao' },
         { key: 'deliverables', label: 'Entregaveis' },
         { key: 'memory',       label: 'Memoria IA' },
         { key: 'dados',        label: 'Dados' },
         ```
       - Keep the AnyPriority type export unchanged.

    2. In `ConsultoriaDetailPage.tsx`:
       - Remove imports: ConsultoriaDetailJornada, ConsultoriaDetailComingSoon
       - Add import: `import { ConsultoriaDetailMemory } from '../ConsultoriaDetailMemory';`
       - In the tab content rendering block (inside AnimatePresence), remove ALL of these lines:
         - `activeTab === 'jornada'` conditional
         - `activeTab === 'mercado'` conditional
         - `activeTab === 'conteudo'` conditional
         - `activeTab === 'financeiro'` conditional
       - Add the memory tab conditional:
         `{activeTab === 'memory' && id && <ConsultoriaDetailMemory consultancyId={id} />}`
       - Place it between deliverables and dados conditionals to match tab order.
       - Keep all other tab conditionals (overview, dados, diagnosis, meetings, actions, deliverables, ai, documentos) exactly as they are.

    Note: ConsultoriaDetailTabs.tsx reads from the TABS array import and needs NO changes — it renders whatever TABS contains.
    Note: useConsultoriaDetail.ts default activeTab is 'overview' which remains correct.
  </action>
  <verify>
    <automated>cd /Users/leonardorodrigues/dev/estrategize-saas && npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "jornada\|mercado\|conteudo\|financeiro" frontend/src/features/consultorias/consultorias.detail.types.ts returns 0
    - grep -c "'memory'" frontend/src/features/consultorias/consultorias.detail.types.ts returns at least 1
    - grep "key:" frontend/src/features/consultorias/consultorias.detail.types.ts shows exactly 9 entries
    - grep "ConsultoriaDetailJornada\|ConsultoriaDetailComingSoon" frontend/src/features/consultorias/components/ConsultoriaDetailPage/ConsultoriaDetailPage.tsx returns 0 matches
    - grep "ConsultoriaDetailMemory" frontend/src/features/consultorias/components/ConsultoriaDetailPage/ConsultoriaDetailPage.tsx returns at least 1 match
    - grep "activeTab === 'memory'" frontend/src/features/consultorias/components/ConsultoriaDetailPage/ConsultoriaDetailPage.tsx returns 1 match
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>TabKey has exactly 9 keys, TABS array has 9 entries in target order, page routes all 9 tabs correctly, removed tabs produce no dead code, TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Adapt ConsultoriaDetailMemory from sidebar to full-width tab layout</name>
  <files>
    frontend/src/features/consultorias/components/ConsultoriaDetailMemory/ConsultoriaDetailMemory.tsx
  </files>
  <read_first>
    frontend/src/features/consultorias/components/ConsultoriaDetailMemory/ConsultoriaDetailMemory.tsx,
    frontend/src/features/consultorias/components/ConsultoriaDetailMemory/MemoryAddForm.tsx
  </read_first>
  <action>
    The ConsultoriaDetailMemory component is currently styled as a narrow sidebar panel (lg:w-80 shrink-0). It needs to work as a full-width tab content area.

    Changes to ConsultoriaDetailMemory.tsx:
    1. Replace the outer container classes:
       - FROM: `lg:w-80 shrink-0 rounded-[var(--radius-md)] bg-[var(--bg-surface-1)] border border-[var(--border-hairline)] flex flex-col overflow-hidden`
       - TO: `rounded-[var(--radius-md)] bg-[var(--bg-surface-1)] border border-[var(--border-hairline)] flex flex-col overflow-hidden`
       (just remove `lg:w-80 shrink-0`)

    2. Change the memory items container from single-column scroll to a responsive grid:
       - The `div` wrapping memory items (currently `flex-1 overflow-y-auto p-3 space-y-2`) should become:
         `p-4 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3`
       - Remove `flex-1 overflow-y-auto` since it's no longer in a sidebar with constrained height.

    3. Update the empty state container:
       - Remove `h-full` from the empty state div (was needed for sidebar flex centering)
       - Keep `flex flex-col items-center justify-center gap-2 py-8 text-center` but wrap in `col-span-full` so it spans the grid:
         `col-span-full flex flex-col items-center justify-center gap-2 py-8 text-center`

    4. Update the loading skeleton:
       - Change from vertical stack `space-y-2` with 3 items to use `contents` wrapper so skeletons participate in grid:
         ```
         <>{[1, 2, 3].map((i) => <Skeleton key={i} className="h-16 w-full" />)}</>
         ```
         (Use fragment, not div, so each skeleton is a grid child)

    Keep all existing functionality: add form toggle, delete mutation, category badges, etc. unchanged.
    Keep file under 80 lines.
  </action>
  <verify>
    <automated>cd /Users/leonardorodrigues/dev/estrategize-saas && npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "lg:w-80" frontend/src/features/consultorias/components/ConsultoriaDetailMemory/ConsultoriaDetailMemory.tsx returns 0 matches
    - grep "grid-cols" frontend/src/features/consultorias/components/ConsultoriaDetailMemory/ConsultoriaDetailMemory.tsx returns at least 1 match
    - grep "col-span-full" frontend/src/features/consultorias/components/ConsultoriaDetailMemory/ConsultoriaDetailMemory.tsx returns at least 1 match
    - wc -l shows file is under 80 lines
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>ConsultoriaDetailMemory renders as full-width responsive grid (1/2/3 cols) instead of narrow sidebar. All functionality preserved.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit --project frontend/tsconfig.json` passes with zero errors
2. `npm run build --prefix frontend` succeeds
3. No references to removed tab keys (jornada, mercado, conteudo, financeiro) exist in types file
4. Tab bar shows exactly 9 tabs in the correct order when app loads
</verification>

<success_criteria>
- 9 tabs in exact order: Visao Geral, Chat IA, Reunioes, Documentos, Diagnostico, Plano de Acao, Entregaveis, Memoria IA, Dados
- Clicking each tab renders the correct component
- Memory tab shows responsive grid of memory items (not narrow sidebar)
- No TypeScript errors, no dead imports
</success_criteria>

<output>
After completion, create `.planning/phases/19-central-da-cliente-tabs/19-01-SUMMARY.md`
</output>
