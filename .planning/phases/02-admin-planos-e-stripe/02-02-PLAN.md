---
phase: 02-admin-planos-e-stripe
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - frontend/src/api/stripe.ts
  - frontend/src/features/admin/hooks/useStripeProducts.ts
  - frontend/src/features/admin/components/AdminPlanosPage/AdminPlanosPage.tsx
  - frontend/src/features/admin/components/AdminPlanosPage/index.ts
  - frontend/src/features/admin/components/PlanCard/PlanCard.tsx
  - frontend/src/features/admin/components/PlanCard/index.ts
  - frontend/src/features/admin/components/PlanFormModal/PlanFormModal.tsx
  - frontend/src/features/admin/components/PlanFormModal/index.ts
autonomous: true
requirements: [ADMN-01, ADMN-07]

must_haves:
  truths:
    - "Admin sees list of plans with name, price, credits, billing interval, and status"
    - "Admin clicks 'Novo Plano' and a modal form appears"
    - "Submitting the form creates the plan in Stripe and shows it in the list"
    - "Admin can archive a plan from the card"
    - "Admin can edit a plan name/credits from the card"
  artifacts:
    - path: "frontend/src/api/stripe.ts"
      provides: "Typed API client for admin/stripe/products"
      exports: ["adminListProducts", "adminCreateProduct", "adminUpdateProduct", "adminArchiveProduct"]
    - path: "frontend/src/features/admin/hooks/useStripeProducts.ts"
      provides: "React Query hook for Stripe products CRUD"
      exports: ["useStripeProducts"]
    - path: "frontend/src/features/admin/components/AdminPlanosPage/AdminPlanosPage.tsx"
      provides: "Aggregator orchestrating PlanCard grid + PlanFormModal"
      min_lines: 40
    - path: "frontend/src/features/admin/components/PlanCard/PlanCard.tsx"
      provides: "Single plan card micro-module"
      min_lines: 20
    - path: "frontend/src/features/admin/components/PlanFormModal/PlanFormModal.tsx"
      provides: "Create/edit plan form modal"
      min_lines: 30
  key_links:
    - from: "frontend/src/features/admin/hooks/useStripeProducts.ts"
      to: "frontend/src/api/stripe.ts"
      via: "import and useQuery/useMutation"
      pattern: "adminListProducts|adminCreateProduct"
    - from: "frontend/src/features/admin/components/AdminPlanosPage/AdminPlanosPage.tsx"
      to: "frontend/src/features/admin/hooks/useStripeProducts.ts"
      via: "useStripeProducts hook"
      pattern: "useStripeProducts"
    - from: "frontend/src/features/admin/components/AdminPlanosPage/AdminPlanosPage.tsx"
      to: "frontend/src/features/admin/components/PlanCard/PlanCard.tsx"
      via: "renders PlanCard in grid"
      pattern: "PlanCard"
---

<objective>
Build the frontend AdminPlanosPage: API client, React Query hook, aggregator, and micro-modules (PlanCard + PlanFormModal).

Purpose: Replace the placeholder AdminPlanosPage with a functional plan management interface. Admin (Iris) creates/edits/archives Stripe plans directly from the browser.
Output: API client, hook, aggregator, 2 micro-modules, barrel exports.
</objective>

<execution_context>
@/Users/leonardorodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leonardorodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-admin-planos-e-stripe/02-01-SUMMARY.md

@frontend/src/api/client.ts
@frontend/src/api/courses.ts (pattern for API client functions)
@frontend/src/features/admin/components/AdminPlanosPage/AdminPlanosPage.tsx (current placeholder to replace)
@frontend/src/features/admin/components/AdminStripePage/AdminStripePage.tsx (aggregator pattern reference)
@frontend/src/features/admin/components/AdminStripePage/WebhooksTab.tsx (micro-module pattern reference)
@frontend/src/lib/motion.ts
@frontend/src/lib/cn.ts
</context>

<interfaces>
<!-- Backend API contract from Plan 01 -->

POST /api/admin/stripe/products
Request: { name: string; description?: string; price_cents: number; credits: number; billing_interval: 'month' | 'year' | 'one_time' }
Response: { data: StripeProduct }

GET /api/admin/stripe/products
Response: { data: StripeProduct[] }

PATCH /api/admin/stripe/products/:id
Request: { name?: string; description?: string; credits?: number; status?: 'active' | 'archived' }
Response: { data: StripeProduct }

DELETE /api/admin/stripe/products/:id
Response: { data: StripeProduct }

StripeProduct = {
  id: string; name: string; description: string | null;
  stripe_product_id: string | null; stripe_price_id: string | null;
  price_cents: number; credits: number;
  billing_interval: 'month' | 'year' | 'one_time';
  status: 'active' | 'archived';
  created_at: string; updated_at: string;
}

<!-- Existing frontend patterns -->

From frontend/src/api/client.ts:
```typescript
export const client = {
  get:    (path: string, opts?) => makeRequest('GET', path, opts),
  post:   (path: string, opts?) => makeRequest('POST', path, opts),
  patch:  (path: string, opts?) => makeRequest('PATCH', path, opts),
  delete: (path: string, opts?) => makeRequest('DELETE', path, opts),
};
// Usage: client.get('/api/foo').json<T>()
```

From frontend/src/lib/motion.ts:
```typescript
export const staggerContainer: Variants;
export const staggerItem: Variants;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: API client + React Query hook</name>
  <files>frontend/src/api/stripe.ts, frontend/src/features/admin/hooks/useStripeProducts.ts</files>
  <read_first>
    - frontend/src/api/client.ts (client interface)
    - frontend/src/api/courses.ts (pattern for typed API functions, first 60 lines)
    - frontend/src/features/admin/components/AdminStripePage/AdminStripePage.tsx (useQuery pattern)
  </read_first>
  <action>
1. Create `frontend/src/api/stripe.ts` (max 80 lines):
   - Import `client` from './client.ts'
   - Define and export `StripeProduct` interface:
     ```
     export interface StripeProduct {
       id: string;
       name: string;
       description: string | null;
       stripe_product_id: string | null;
       stripe_price_id: string | null;
       price_cents: number;
       credits: number;
       billing_interval: 'month' | 'year' | 'one_time';
       status: 'active' | 'archived';
       created_at: string;
       updated_at: string;
     }
     ```
   - Define and export `CreateProductInput`:
     ```
     export interface CreateProductInput {
       name: string;
       description?: string;
       price_cents: number;
       credits: number;
       billing_interval: 'month' | 'year' | 'one_time';
     }
     ```
   - Define and export `UpdateProductInput`:
     ```
     export interface UpdateProductInput {
       name?: string;
       description?: string;
       credits?: number;
       status?: 'active' | 'archived';
     }
     ```
   - Export functions:
     - `adminListProducts()`: `client.get('/api/admin/stripe/products').json<{ data: StripeProduct[] }>()`
     - `adminCreateProduct(input: CreateProductInput)`: `client.post('/api/admin/stripe/products', { json: input }).json<{ data: StripeProduct }>()`
     - `adminUpdateProduct(id: string, input: UpdateProductInput)`: `client.patch(`/api/admin/stripe/products/${id}`, { json: input }).json<{ data: StripeProduct }>()`
     - `adminArchiveProduct(id: string)`: `client.delete(`/api/admin/stripe/products/${id}`).json<{ data: StripeProduct }>()`

2. Create `frontend/src/features/admin/hooks/useStripeProducts.ts` (max 80 lines):
   - Import `useQuery`, `useMutation`, `useQueryClient` from '@tanstack/react-query'
   - Import all 4 API functions + types from '../../../api/stripe.ts'
   - Export `useStripeProducts()` hook that returns:
     - `products`: StripeProduct[] from useQuery with queryKey `['admin-stripe-products']`
     - `isLoading`: boolean
     - `createProduct`: useMutation wrapping `adminCreateProduct`, invalidates `['admin-stripe-products']` on success
     - `updateProduct`: useMutation wrapping `adminUpdateProduct`, invalidates on success
     - `archiveProduct`: useMutation wrapping `adminArchiveProduct`, invalidates on success
     - `isCreating`, `isUpdating`, `isArchiving`: boolean loading states from mutations
  </action>
  <verify>
    <automated>cd /Users/leonardorodrigues/dev/estrategize-saas/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - File `stripe.ts` exports `StripeProduct`, `CreateProductInput`, `UpdateProductInput`
    - File `stripe.ts` exports `adminListProducts`, `adminCreateProduct`, `adminUpdateProduct`, `adminArchiveProduct`
    - File `useStripeProducts.ts` exports `useStripeProducts`
    - File `useStripeProducts.ts` imports from `../../../api/stripe.ts`
    - File `useStripeProducts.ts` contains `useQuery` and `useMutation`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Typed Stripe API client and React Query hook ready. 4 API functions, 1 hook with query + 3 mutations.</done>
</task>

<task type="auto">
  <name>Task 2: PlanCard + PlanFormModal micro-modules + AdminPlanosPage aggregator</name>
  <files>
    frontend/src/features/admin/components/PlanCard/PlanCard.tsx,
    frontend/src/features/admin/components/PlanCard/index.ts,
    frontend/src/features/admin/components/PlanFormModal/PlanFormModal.tsx,
    frontend/src/features/admin/components/PlanFormModal/index.ts,
    frontend/src/features/admin/components/AdminPlanosPage/AdminPlanosPage.tsx,
    frontend/src/features/admin/components/AdminPlanosPage/index.ts
  </files>
  <read_first>
    - frontend/src/features/admin/hooks/useStripeProducts.ts (just created in Task 1)
    - frontend/src/api/stripe.ts (types StripeProduct, CreateProductInput)
    - frontend/src/features/admin/components/AdminStripePage/AdminStripePage.tsx (aggregator pattern)
    - frontend/src/features/admin/components/AdminStripePage/WebhooksTab.tsx (micro-module pattern)
    - frontend/src/components/ui/ (list available primitives: Button, Modal, Input, Badge)
    - frontend/src/lib/motion.ts (staggerContainer, staggerItem)
    - frontend/src/lib/cn.ts
  </read_first>
  <action>
1. Create `PlanCard/PlanCard.tsx` (max 80 lines):
   - Props interface `PlanCardProps`:
     - `plan: StripeProduct` (import from api/stripe.ts)
     - `onEdit: (plan: StripeProduct) => void`
     - `onArchive: (id: string) => void`
     - `isArchiving: boolean`
   - Renders a card with:
     - Plan name as title (text-sm font-semibold)
     - Price formatted: `(plan.price_cents / 100).toLocaleString('pt-BR', { style: 'currency', currency: 'BRL' })` + billing interval label (mensal/anual/unico)
     - Credits badge: `{plan.credits} creditos`
     - Status badge: 'active' = green-ish using design tokens, 'archived' = muted
     - Two action buttons: "Editar" (calls onEdit) and "Arquivar" (calls onArchive, disabled if already archived or isArchiving)
   - Use design tokens: `var(--bg-surface-1)`, `var(--border-hairline)`, `var(--text-primary)`, `var(--text-secondary)`, `var(--text-tertiary)`
   - Named export: `export function PlanCard`

2. Create `PlanCard/index.ts`: `export { PlanCard } from './PlanCard';`

3. Create `PlanFormModal/PlanFormModal.tsx` (max 80 lines):
   - Props interface `PlanFormModalProps`:
     - `isOpen: boolean`
     - `onClose: () => void`
     - `onSubmit: (input: CreateProductInput) => void`
     - `isSubmitting: boolean`
     - `editPlan?: StripeProduct | null` (for edit mode - pre-fills name/description/credits)
   - Uses local state for form fields: name, description, price_cents (display as reais, store as cents), credits, billing_interval
   - If `editPlan` provided, pre-fill fields on mount/change (useEffect)
   - On submit: validate name is non-empty, price >= 0, credits >= 0, then call `onSubmit({ name, description, price_cents, credits, billing_interval })`
   - Modal overlay pattern: fixed inset-0 bg-black/50 z-50, centered card
   - Form fields: name (text input), description (text input), price (number input, user types in BRL like "97.00" which converts to 9700 cents), credits (number), billing_interval (select with options: Mensal/Anual/Pagamento Unico)
   - Submit button text: editPlan ? "Salvar" : "Criar Plano" — disabled when isSubmitting
   - Named export: `export function PlanFormModal`

4. Create `PlanFormModal/index.ts`: `export { PlanFormModal } from './PlanFormModal';`

5. Replace `AdminPlanosPage/AdminPlanosPage.tsx` (max 200 lines) with real aggregator:
   - Import `useStripeProducts` from hooks
   - Import `PlanCard` from '../PlanCard'
   - Import `PlanFormModal` from '../PlanFormModal'
   - Import `staggerContainer`, `staggerItem` from lib/motion.ts
   - Import `motion` from 'framer-motion'
   - Local state: `isModalOpen: boolean`, `editingPlan: StripeProduct | null`
   - Destructure from hook: `products, isLoading, createProduct, updateProduct, archiveProduct, isCreating, isUpdating`
   - Header section: title "Planos e Stripe", subtitle "Gerencie planos de assinatura.", "Novo Plano" button (opens modal)
   - Content:
     - Loading state: 3 skeleton cards (animate-pulse)
     - Empty state: message "Nenhum plano cadastrado." with CTA button
     - Grid of PlanCard components: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4`
     - Pass `onEdit={(plan) => { setEditingPlan(plan); setIsModalOpen(true); }}` and `onArchive={(id) => archiveProduct.mutate(id)}`
   - PlanFormModal:
     - `isOpen={isModalOpen}`
     - `onClose={() => { setIsModalOpen(false); setEditingPlan(null); }}`
     - `onSubmit`: if editingPlan, call `updateProduct.mutate({ id, input })` then close; else call `createProduct.mutate(input)` then close
     - `isSubmitting={isCreating || isUpdating}`
     - `editPlan={editingPlan}`
   - Use motion wrappers with staggerContainer/staggerItem for smooth entry

6. Verify `AdminPlanosPage/index.ts` already exports correctly (it does: `export { AdminPlanosPage } from './AdminPlanosPage';`)
  </action>
  <verify>
    <automated>cd /Users/leonardorodrigues/dev/estrategize-saas/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - File `PlanCard.tsx` exists and is <= 80 lines
    - File `PlanFormModal.tsx` exists and is <= 80 lines
    - File `AdminPlanosPage.tsx` exists and is <= 200 lines
    - File `AdminPlanosPage.tsx` imports `useStripeProducts`
    - File `AdminPlanosPage.tsx` imports `PlanCard` and `PlanFormModal`
    - File `AdminPlanosPage.tsx` does NOT import `supabase` or call `fetch` directly
    - Barrel `index.ts` files exist for PlanCard and PlanFormModal
    - Page shell `src/pages/admin/AdminPlanosPage.tsx` remains <= 5 lines (untouched)
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>AdminPlanosPage is a functional aggregator with PlanCard grid and PlanFormModal. 3-layer architecture enforced: page (1 line re-export), aggregator (<=200L), micro-modules (<=80L each).</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes in frontend/
- AdminPlanosPage aggregator <= 200 lines, micro-modules <= 80 lines each
- Page shell remains 1-line re-export
- No direct fetch/supabase calls in components
- PlanCard receives data via props, emits via callbacks
- PlanFormModal receives onSubmit callback, does not call API directly
- Hook isolates all React Query logic
</verification>

<success_criteria>
Admin can view plans in a card grid, create new plans via modal form (which calls Stripe API through backend), edit plan name/credits, and archive plans. All frontend code follows 3-layer architecture with proper separation of concerns.
</success_criteria>

<output>
After completion, create `.planning/phases/02-admin-planos-e-stripe/02-02-SUMMARY.md`
</output>
