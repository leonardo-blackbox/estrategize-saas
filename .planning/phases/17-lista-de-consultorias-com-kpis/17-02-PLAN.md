---
phase: 17-lista-de-consultorias-com-kpis
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - frontend/src/features/consultorias/components/ConsultoriasEmptyState/ConsultoriasEmptyState.tsx
autonomous: true
requirements:
  - CONS-02

must_haves:
  truths:
    - "Quando não há consultorias, o empty state exibe uma headline de value proposition clara"
    - "O CTA 'Criar primeira consultoria' está visível e aciona o modal de criação"
    - "Quando há busca/filtro ativo sem resultados, o empty state exibe mensagem contextual diferente (sem CTA)"
  artifacts:
    - path: "frontend/src/features/consultorias/components/ConsultoriasEmptyState/ConsultoriasEmptyState.tsx"
      provides: "Empty state com copy de value proposition"
      contains: "centralizada"
  key_links:
    - from: "ConsultoriasEmptyState"
      to: "onCreateClick prop"
      via: "Button onClick chama onCreateClick"
      pattern: "onCreateClick"
---

<objective>
Reescrever o copy do ConsultoriasEmptyState com value proposition forte e CTA claro.

Purpose: O empty state atual ("Nenhuma consultoria ativa. / Crie uma nova para começar.") não comunica valor — uma consultora nova não entende por que deveria criar algo. O novo copy deve vender o benefício imediato da ferramenta.
Output: 1 arquivo modificado; estrutura de componente e props mantidas idênticas.
</objective>

<execution_context>
@/Users/leonardorodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leonardorodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@frontend/src/features/consultorias/components/ConsultoriasEmptyState/ConsultoriasEmptyState.tsx

<interfaces>
<!-- Interface atual do componente — manter sem alteração -->

```typescript
interface ConsultoriasEmptyStateProps {
  hasSearch: boolean;
  onCreateClick: () => void;
}
```

O componente recebe:
- `hasSearch: boolean` — true quando há busca ou filtro ativo (exibe mensagem sem CTA)
- `onCreateClick: () => void` — chamado ao clicar no botão de criar

Importações existentes a manter:
```typescript
import { Button } from '../../../../components/ui/Button.tsx';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescrever copy do ConsultoriasEmptyState com value proposition</name>
  <files>
    frontend/src/features/consultorias/components/ConsultoriasEmptyState/ConsultoriasEmptyState.tsx
  </files>
  <action>
Reescrever o JSX mantendo a estrutura exata do componente (ícone + texto + botão condicional), alterando apenas o copy e refinando o visual do ícone.

**Estado sem busca (`!hasSearch`) — value proposition:**
- Headline: `"Sua base de clientes, centralizada."`
- Supporting text: `"Acompanhe progresso, próximas reuniões e prioridades de todos os seus clientes em um só lugar."`
- CTA: `"Criar primeira consultoria"` (manter Button com `size="sm"`)

**Estado com busca ativa (`hasSearch`) — contextual:**
- Texto principal: `"Nenhum resultado para este filtro."`
- Texto secundário: `"Tente outro termo ou ajuste os filtros de etapa e status."`
- Sem botão CTA

**Estrutura JSX alvo (manter CSS variables e tokens existentes):**
```tsx
<div className="flex flex-col items-center justify-center gap-4 py-16 text-center">
  <div className="h-12 w-12 rounded-full bg-[var(--bg-surface-1)] border border-[var(--border-hairline)] flex items-center justify-center">
    {/* manter o SVG de briefcase existente */}
  </div>
  <div className="max-w-xs">
    <p className="text-sm font-semibold text-[var(--text-primary)]">
      {hasSearch ? 'Nenhum resultado para este filtro.' : 'Sua base de clientes, centralizada.'}
    </p>
    <p className="text-xs text-[var(--text-tertiary)] mt-1 leading-relaxed">
      {hasSearch
        ? 'Tente outro termo ou ajuste os filtros de etapa e status.'
        : 'Acompanhe progresso, próximas reuniões e prioridades de todos os seus clientes em um só lugar.'}
    </p>
  </div>
  {!hasSearch && (
    <Button size="sm" onClick={onCreateClick}>
      Criar primeira consultoria
    </Button>
  )}
</div>
```

Diferenças em relação ao atual:
- `gap-3` → `gap-4` para dar mais respiro
- `font-medium` → `font-semibold` na headline
- `mt-0.5` → `mt-1 leading-relaxed` no supporting text
- `max-w-xs` adicionado ao `<div>` de texto para limitar largura
- Copy completamente reescrito conforme acima
- Componente deve ficar ≤80 linhas
  </action>
  <verify>
    <automated>cd /Users/leonardorodrigues/dev/estrategize-saas/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - Arquivo modificado com o novo copy.
    - TypeScript compila sem erros.
    - `hasSearch=false` exibe headline "Sua base de clientes, centralizada." e botão "Criar primeira consultoria".
    - `hasSearch=true` exibe "Nenhum resultado para este filtro." sem botão.
    - Componente ≤80 linhas.
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/leonardorodrigues/dev/estrategize-saas/frontend && npx tsc --noEmit
```
Zero erros. Verificar visualmente (se disponível): abrir `/consultorias` com conta sem consultorias e confirmar novo copy; buscar por termo inexistente e confirmar mensagem contextual.
</verification>

<success_criteria>
- `ConsultoriasEmptyState` exibe headline de value proposition quando `hasSearch=false`
- Supporting text descreve o benefício da ferramenta (não instruções de ação)
- CTA diz "Criar primeira consultoria" (não "Criar Consultoria")
- Estado com `hasSearch=true` exibe texto contextual sem CTA
- `npx tsc --noEmit` passa sem erros
- Componente ≤80 linhas
</success_criteria>

<output>
Após a conclusão, criar `.planning/phases/17-lista-de-consultorias-com-kpis/17-02-SUMMARY.md`
</output>
