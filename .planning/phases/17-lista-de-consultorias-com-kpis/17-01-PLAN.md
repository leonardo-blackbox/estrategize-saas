---
phase: 17-lista-de-consultorias-com-kpis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/consultorias/consultorias.helpers.ts
  - frontend/src/features/consultorias/hooks/useConsultorias.ts
  - frontend/src/hooks/useDebounce.ts
  - frontend/src/features/consultorias/components/ConsultoriaCard/ConsultoriaCard.tsx
  - frontend/src/features/consultorias/components/ConsultoriasFilterBar/ConsultoriasFilterBar.tsx
  - frontend/src/features/consultorias/components/ConsultoriasPage/ConsultoriasPage.tsx
autonomous: true
requirements:
  - CONS-01
  - CONS-02

must_haves:
  truths:
    - "Card exibe @instagram do cliente logo abaixo do client_name quando o campo não é null"
    - "Filtro por status (Todas / Em risco / Alta prioridade) reduz a lista de consultorias exibidas"
    - "Pills de status aparecem na FilterBar sem quebrar o layout existente das phase pills"
  artifacts:
    - path: "frontend/src/features/consultorias/consultorias.helpers.ts"
      provides: "StatusFilter type + statusFilterLabels array"
      contains: "export type StatusFilter"
    - path: "frontend/src/features/consultorias/hooks/useConsultorias.ts"
      provides: "statusFilter state + .filter() por priority no array filtered"
      contains: "statusFilter"
    - path: "frontend/src/hooks/useDebounce.ts"
      provides: "Generic debounce hook extraído de useConsultorias"
      contains: "export function useDebounce"
    - path: "frontend/src/features/consultorias/components/ConsultoriaCard/ConsultoriaCard.tsx"
      provides: "Renderização do @instagram"
      contains: "c.instagram"
    - path: "frontend/src/features/consultorias/components/ConsultoriasFilterBar/ConsultoriasFilterBar.tsx"
      provides: "Status pills row"
      contains: "statusFilter"
  key_links:
    - from: "useConsultorias"
      to: "ConsultoriasPage"
      via: "statusFilter / setStatusFilter retornados pelo hook e passados como props para ConsultoriasFilterBar"
      pattern: "statusFilter.*setStatusFilter"
    - from: "consultorias.helpers.ts"
      to: "useConsultorias + ConsultoriasFilterBar"
      via: "StatusFilter type + statusFilterLabels importados"
      pattern: "StatusFilter|statusFilterLabels"
    - from: "useConsultorias"
      to: "ConsultoriasPage"
      via: "hasSearch expression inclui statusFilter e phaseFilter além de debouncedSearch"
      pattern: "debouncedSearch.*statusFilter.*phaseFilter"
---

<objective>
Adicionar @instagram no ConsultoriaCard, filtro por status (at_risk/high) no hook e pills de status na FilterBar.

Purpose: Completar os critérios de sucesso CONS-01 e CONS-02 — a consultora precisa ver o handle do Instagram de cada cliente no card e poder filtrar por clientes em risco ou alta prioridade de relance.
Output: 6 arquivos modificados (1 novo: useDebounce.ts extraído para manter useConsultorias dentro do limite de 120 linhas).
</objective>

<execution_context>
@/Users/leonardorodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leonardorodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend/src/features/consultorias/consultorias.helpers.ts
@frontend/src/features/consultorias/hooks/useConsultorias.ts
@frontend/src/features/consultorias/components/ConsultoriaCard/ConsultoriaCard.tsx
@frontend/src/features/consultorias/components/ConsultoriasFilterBar/ConsultoriasFilterBar.tsx
@frontend/src/features/consultorias/components/ConsultoriasPage/ConsultoriasPage.tsx

<interfaces>
<!-- Tipos relevantes do codebase para este plano -->

From frontend/src/api/consultancies.ts (via features/consultorias/services/consultorias.api.ts re-export):
```typescript
export type ConsultancyPriority = 'low' | 'normal' | 'high' | 'at_risk';

export interface Consultancy {
  id: string;
  client_name: string | null;
  title: string;
  status: 'active' | 'archived';
  phase: ConsultancyPhase | null;
  niche: string | null;
  instagram: string | null;          // ← campo a exibir no card
  next_meeting_at: string | null;
  priority: ConsultancyPriority | null;
  implementation_score: number | null;
  credits_spent: number;
  // ...
}
```

From frontend/src/features/consultorias/consultorias.helpers.ts (estado atual):
```typescript
export type SortOption = 'recent' | 'priority' | 'progress' | 'alpha';
export type PhaseFilter = ConsultancyPhase | 'all';
export const PRIORITY_WEIGHT: Record<string, number> = { at_risk: 0, high: 1, normal: 2, low: 3 };
export const phaseFilterLabels: { value: PhaseFilter; label: string }[] = [...];
```

From useConsultorias.ts — filtered pipeline atual:
```typescript
const filtered = consultancies
  .filter((c) => c.status !== 'archived')
  .filter((c) => phaseFilter === 'all' || c.phase === phaseFilter)
  .filter((c) => { /* search */ })
  .sort(...);
```

From ConsultoriasPage.tsx — props actuais para FilterBar:
```typescript
<ConsultoriasFilterBar
  search={search}
  onSearchChange={setSearch}
  sortBy={sortBy}
  onSortChange={setSortBy}
  phaseFilter={phaseFilter}
  onPhaseFilterChange={setPhaseFilter}
/>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extrair useDebounce, adicionar StatusFilter ao helpers e ao useConsultorias hook</name>
  <files>
    frontend/src/hooks/useDebounce.ts
    frontend/src/features/consultorias/consultorias.helpers.ts
    frontend/src/features/consultorias/hooks/useConsultorias.ts
  </files>
  <action>
**PASSO 0 — Criar frontend/src/hooks/useDebounce.ts (novo arquivo):**
Extrair a lógica de debounce que atualmente existe inline em useConsultorias.ts para um hook genérico reutilizável. O arquivo deve ter no máximo 20 linhas:

```typescript
import { useState, useEffect } from 'react';

export function useDebounce<T>(value: T, delay: number): T {
  const [debounced, setDebounced] = useState<T>(value);

  useEffect(() => {
    const timer = setTimeout(() => setDebounced(value), delay);
    return () => clearTimeout(timer);
  }, [value, delay]);

  return debounced;
}
```

**Em consultorias.helpers.ts:**
Adicionar logo após a definição de `PhaseFilter`:

```typescript
export type StatusFilter = 'all' | 'at_risk' | 'high';

export const statusFilterLabels: { value: StatusFilter; label: string }[] = [
  { value: 'all',     label: 'Todas'          },
  { value: 'at_risk', label: 'Em risco'        },
  { value: 'high',    label: 'Alta prioridade' },
];
```

**Em useConsultorias.ts:**
1. Remover o `useEffect`/`useState` de debounce inline e substituir pelo import de `useDebounce` de `../../../hooks/useDebounce` — isso economiza ~6 linhas e mantém o arquivo dentro do limite de 120 linhas.
2. Importar `StatusFilter` de `consultorias.helpers.ts` (adicionar ao import existente).
3. Adicionar estado: `const [statusFilter, setStatusFilter] = useState<StatusFilter>('all');` — colocar junto dos demais `useState` no topo da função.
4. No pipeline `filtered`, adicionar uma `.filter()` INLINE antes do `.sort()`:
   ```typescript
   .filter((c) => statusFilter === 'all' || c.priority === statusFilter)
   ```
5. Retornar `statusFilter` e `setStatusFilter` no objeto de retorno do hook.
6. Verificar contagem de linhas: o arquivo resultante deve ter ≤120 linhas (limite CLAUDE.md).
  </action>
  <verify>
    <automated>cd /Users/leonardorodrigues/dev/estrategize-saas/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    TypeScript compila sem erros em helpers.ts e useConsultorias.ts; `StatusFilter` e `statusFilterLabels` estão exportados; hook retorna `statusFilter` e `setStatusFilter`; `filtered` array respeita o `statusFilter`; `useDebounce.ts` existe em `src/hooks/` e é importado por `useConsultorias.ts`; `useConsultorias.ts` tem ≤120 linhas. Com phaseFilter e statusFilter ambos não-'all', o array filtrado satisfaz ambas as restrições simultaneamente (lógica AND, não OR nem substituição).
  </done>
</task>

<task type="auto">
  <name>Task 2: Exibir @instagram no ConsultoriaCard e adicionar status pills na ConsultoriasFilterBar</name>
  <files>
    frontend/src/features/consultorias/components/ConsultoriaCard/ConsultoriaCard.tsx
    frontend/src/features/consultorias/components/ConsultoriasFilterBar/ConsultoriasFilterBar.tsx
    frontend/src/features/consultorias/components/ConsultoriasPage/ConsultoriasPage.tsx
  </files>
  <action>
**ConsultoriaCard.tsx (atualmente 76 linhas — manter ≤80L):**
Na linha que renderiza `c.client_name` (dentro do `<div className="min-w-0">`), adicionar imediatamente após o `<p>` do client_name e ANTES do `<p>` do `c.title`:

```tsx
{c.instagram && (
  <p className="text-[11px] text-[var(--text-muted)] truncate leading-tight">@{c.instagram}</p>
)}
```

A estrutura final da seção de nome fica:
```
client_name (font-semibold, --text-primary)
@instagram   (text-muted, condicional)
title        (text-tertiary)
```

**ConsultoriasFilterBar.tsx (atualmente 72 linhas — manter ≤80L):**
1. Atualizar a interface `ConsultoriasFilterBarProps` adicionando:
   ```typescript
   statusFilter: StatusFilter;
   onStatusFilterChange: (value: StatusFilter) => void;
   ```
2. Importar `StatusFilter` e `statusFilterLabels` de `consultorias.helpers.ts` (adicionar ao import existente da linha 4).
3. Após o bloco `{/* Phase pills */}` existente, adicionar um segundo bloco:
   ```tsx
   {/* Status pills */}
   <div className="flex gap-1 flex-wrap">
     {statusFilterLabels.map(({ value, label }) => (
       <button
         key={value}
         onClick={() => onStatusFilterChange(value)}
         className={cn(
           'px-3 py-1 rounded-[var(--radius-sm)] text-[12px] font-medium transition-colors',
           statusFilter === value
             ? 'bg-[var(--bg-surface-2)] text-[var(--text-primary)]'
             : 'text-[var(--text-tertiary)] hover:text-[var(--text-secondary)] hover:bg-[var(--bg-hover)]',
         )}
       >
         {label}
       </button>
     ))}
   </div>
   ```

**ConsultoriasPage.tsx (aggregator — manter ≤200L):**
1. Adicionar `statusFilter` e `setStatusFilter` ao destructure de `useConsultorias()`.
2. Passar como props para `<ConsultoriasFilterBar>`:
   ```tsx
   statusFilter={statusFilter}
   onStatusFilterChange={setStatusFilter}
   ```
3. Atualizar a expressão `hasSearch` passada para `<ConsultoriasEmptyState>` de `!!debouncedSearch` para:
   ```tsx
   hasSearch={!!debouncedSearch || statusFilter !== 'all' || phaseFilter !== 'all'}
   ```
   Isso garante que quando qualquer filtro está ativo e retorna zero resultados, o empty state mostra "Nenhum resultado para este filtro." em vez da mensagem de value proposition com CTA.
  </action>
  <verify>
    <automated>cd /Users/leonardorodrigues/dev/estrategize-saas/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - TypeScript compila sem erros nos 3 arquivos.
    - ConsultoriaCard exibe `@instagram` entre client_name e title quando o campo não é null.
    - ConsultoriasFilterBar aceita e renderiza as duas linhas de pills (phase + status) sem erro de prop.
    - ConsultoriasPage passa statusFilter/setStatusFilter corretamente.
    - hasSearch é true sempre que qualquer filtro (busca, etapa ou status) estiver ativo — `!!debouncedSearch || statusFilter !== 'all' || phaseFilter !== 'all'`.
    - Nenhum arquivo ultrapassa o limite de linhas da camada (Card ≤80L, FilterBar ≤80L, Page ≤200L).
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/leonardorodrigues/dev/estrategize-saas/frontend && npx tsc --noEmit
```
Zero erros TypeScript. Verificar manualmente na UI (se disponível): abrir `/consultorias`, confirmar que cards com `instagram` preenchido exibem o handle, e que as pills de status filtram a lista. Verificar que ao ativar qualquer filtro sem resultados, o empty state mostra "Nenhum resultado para este filtro." sem o botão CTA.
</verification>

<success_criteria>
- `StatusFilter` type exportado de `consultorias.helpers.ts`
- `statusFilterLabels` com 3 entradas exportado de `consultorias.helpers.ts`
- `useDebounce.ts` criado em `frontend/src/hooks/` e importado por `useConsultorias.ts`
- `useConsultorias` retorna `statusFilter` + `setStatusFilter`; `filtered` aplica filtro por `priority`; arquivo ≤120 linhas
- `ConsultoriaCard` renderiza `@{c.instagram}` condicionalmente entre client_name e title
- `ConsultoriasFilterBar` renderiza segunda linha de pills para status
- `ConsultoriasPage` passa as novas props para FilterBar
- `ConsultoriasPage` usa `hasSearch={!!debouncedSearch || statusFilter !== 'all' || phaseFilter !== 'all'}`
- `npx tsc --noEmit` passa sem erros
</success_criteria>

<output>
Após a conclusão, criar `.planning/phases/17-lista-de-consultorias-com-kpis/17-01-SUMMARY.md`
</output>
