---
phase: 06-pagina-de-planos
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - frontend/src/features/planos/components/PlanosPage/PlanosPage.tsx
  - frontend/src/features/planos/components/PlanosPage/index.ts
  - frontend/src/features/planos/components/PlanCard/PlanCard.tsx
  - frontend/src/features/planos/components/PlanCard/index.ts
  - frontend/src/features/planos/components/index.ts
  - frontend/src/pages/public/PlanosPage.tsx
  - frontend/src/App.tsx
autonomous: true
requirements:
  - CHKT-01

must_haves:
  truths:
    - "Visitante acessa /planos e ve cards de planos sem estar logado"
    - "Cada card mostra nome, preco formatado em BRL, intervalo, creditos e CTA"
    - "Loading state exibe skeleton enquanto dados carregam"
    - "Empty state aparece quando nao ha planos ativos"
    - "Layout funciona em 375px (mobile-first)"
  artifacts:
    - path: "frontend/src/pages/public/PlanosPage.tsx"
      provides: "Route page (max 20 lines)"
      min_lines: 3
    - path: "frontend/src/features/planos/components/PlanosPage/PlanosPage.tsx"
      provides: "Aggregator orchestrating plan cards grid"
      min_lines: 30
    - path: "frontend/src/features/planos/components/PlanCard/PlanCard.tsx"
      provides: "Individual plan card micro-module"
      min_lines: 20
  key_links:
    - from: "frontend/src/pages/public/PlanosPage.tsx"
      to: "frontend/src/features/planos/components/PlanosPage/PlanosPage.tsx"
      via: "import aggregator"
      pattern: "import.*PlanosPage.*from.*features/planos"
    - from: "frontend/src/features/planos/components/PlanosPage/PlanosPage.tsx"
      to: "frontend/src/features/planos/hooks/usePlans.ts"
      via: "hook import"
      pattern: "import.*usePlans.*from.*hooks/usePlans"
    - from: "frontend/src/features/planos/components/PlanosPage/PlanosPage.tsx"
      to: "frontend/src/features/planos/components/PlanCard/PlanCard.tsx"
      via: "micro-module import"
      pattern: "import.*PlanCard.*from.*PlanCard"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/public/PlanosPage.tsx"
      via: "Route element"
      pattern: "Route.*path=.*planos.*element=.*PlanosPage"
---

<objective>
Build the public plans page with card grid, loading state, empty state, and mobile-first layout.

Purpose: Visitors see available plans with pricing before signing up, enabling informed purchase decisions.
Output: Complete /planos page following 3-layer architecture (Page + Aggregator + PlanCard micro-module).
</objective>

<execution_context>
@/Users/leonardorodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leonardorodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-pagina-de-planos/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 06-01 outputs -->

From frontend/src/api/plans.ts:
```typescript
export interface PublicPlan {
  id: string;
  name: string;
  description: string | null;
  price_cents: number;
  credits: number;
  billing_interval: 'month' | 'year' | 'one_time';
}
export function listPublicPlans(): ResponsePromise
```

From frontend/src/features/planos/hooks/usePlans.ts:
```typescript
export function usePlans(): {
  data: PublicPlan[] | undefined;
  isLoading: boolean;
  error: Error | null;
}
```

<!-- Existing patterns from codebase -->

From frontend/src/App.tsx route pattern:
```typescript
// Public routes (no auth wrapper):
<Route path="/f/:slug" element={<FormPublicoPage />} />
// Route sits at top level alongside /login, /signup
```

Design tokens (CSS variables):
```
--bg-base, --bg-surface-1, --border-hairline
--text-primary, --text-secondary, --text-tertiary
--iris-violet: #7c5cfc (brand accent)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PlanCard micro-module and PlanosPage aggregator</name>
  <files>
    frontend/src/features/planos/components/PlanCard/PlanCard.tsx,
    frontend/src/features/planos/components/PlanCard/index.ts,
    frontend/src/features/planos/components/PlanosPage/PlanosPage.tsx,
    frontend/src/features/planos/components/PlanosPage/index.ts,
    frontend/src/features/planos/components/index.ts
  </files>
  <read_first>
    frontend/src/features/admin/components/PlanCard/PlanCard.tsx,
    frontend/src/features/planos/hooks/usePlans.ts,
    frontend/src/api/plans.ts
  </read_first>
  <action>
Create `frontend/src/features/planos/components/PlanCard/PlanCard.tsx` (max 80 lines):
- Import `PublicPlan` from `../../../../api/plans.ts`
- Props: `{ plan: PublicPlan; onSubscribe: (planId: string) => void }`
- Price formatting helper inline: `formatPrice(priceCents: number, interval: string): string`
  - Divide by 100, format with `Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' })`
  - Append suffix: `month` -> `/mes`, `year` -> `/ano`, `one_time` -> ` (unico)`
- Interval label: `month` -> `Mensal`, `year` -> `Anual`, `one_time` -> `Pagamento Unico`
- Card structure using CSS variable tokens:
  - Container: `rounded-2xl border border-[var(--border-hairline)] bg-[var(--bg-surface-1)] p-6 flex flex-col gap-4`
  - Plan name: `text-lg font-semibold text-[var(--text-primary)]`
  - Price: `text-3xl font-bold text-[var(--text-primary)]` with interval suffix in `text-sm font-normal text-[var(--text-secondary)]`
  - Credits line: `text-sm text-[var(--text-secondary)]` showing `{plan.credits} creditos/mes` (or `/ano` for year)
  - Description (if present): `text-sm text-[var(--text-tertiary)]`
  - CTA button: `mt-auto w-full rounded-lg bg-[#7c5cfc] hover:bg-[#6b4ee0] text-white font-medium py-3 px-4 transition-colors` with text "Assinar"
  - onClick CTA calls `onSubscribe(plan.id)`

Create `frontend/src/features/planos/components/PlanCard/index.ts`:
- `export { PlanCard } from './PlanCard';`

Create `frontend/src/features/planos/components/PlanosPage/PlanosPage.tsx` (max 200 lines):
- Import `usePlans` from `../../hooks/usePlans.ts`
- Import `PlanCard` from `../PlanCard`
- Implement 3 states:
  1. **Loading**: grid of 3 skeleton cards (rounded-2xl bg-[var(--bg-surface-1)] animate-pulse h-72)
  2. **Empty**: centered message "Nenhum plano disponivel no momento." with `text-[var(--text-secondary)]`
  3. **Data**: grid of PlanCard components
- Layout:
  - Page wrapper: `min-h-screen bg-[var(--bg-base)] py-12 px-4`
  - Header: centered `text-3xl font-bold text-[var(--text-primary)] mb-2` "Nossos Planos"
  - Subtitle: centered `text-[var(--text-secondary)] mb-10` "Escolha o plano ideal para o seu negocio"
  - Grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 max-w-5xl mx-auto`
- `handleSubscribe(planId: string)`: for now, `console.log('Subscribe:', planId)` — Phase 7 will implement real checkout
- Named export: `export function PlanosPage()`

Create `frontend/src/features/planos/components/PlanosPage/index.ts`:
- `export { PlanosPage } from './PlanosPage';`

Create `frontend/src/features/planos/components/index.ts`:
- `export { PlanosPage } from './PlanosPage';`
- `export { PlanCard } from './PlanCard';`
  </action>
  <verify>
    <automated>cd /Users/leonardorodrigues/dev/estrategize-saas/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
- PlanCard.tsx is under 80 lines, receives `plan` and `onSubscribe` props
- PlanosPage.tsx is under 200 lines, uses `usePlans()` hook
- Loading state renders 3 skeleton cards with `animate-pulse`
- Empty state shows "Nenhum plano disponivel" message
- Grid uses `grid-cols-1 md:grid-cols-2 lg:grid-cols-3`
- Price formatted as BRL currency with interval suffix
- All barrel index.ts files export correctly
  </acceptance_criteria>
  <done>PlanCard renders individual plan data, PlanosPage aggregates cards with loading/empty states, mobile-first grid</done>
</task>

<task type="auto">
  <name>Task 2: Route page and App.tsx wiring</name>
  <files>frontend/src/pages/public/PlanosPage.tsx, frontend/src/App.tsx</files>
  <read_first>frontend/src/App.tsx, frontend/src/pages/public/FormPublicoPage.tsx</read_first>
  <action>
Create `frontend/src/pages/public/PlanosPage.tsx` (max 20 lines):
- Import `PlanosPage as PlanosPageView` from `../../features/planos/components/PlanosPage`
- Export named function `PlanosPage` that renders `<PlanosPageView />`
- This is a thin route page following the 3-layer architecture

Update `frontend/src/App.tsx`:
- Add import at top with other public page imports: `import { PlanosPage } from './pages/public/PlanosPage.tsx';`
- Add route as a public route (no ProtectedRoute wrapper), alongside `/login`, `/signup`, `/f/:slug`:
  `<Route path="/planos" element={<PlanosPage />} />`
- Place it near the other public routes (after `/f/:slug` and before the ProtectedRoute block)
  </action>
  <verify>
    <automated>cd /Users/leonardorodrigues/dev/estrategize-saas/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
- `frontend/src/pages/public/PlanosPage.tsx` is under 20 lines
- Page only imports and renders the aggregator from features/planos
- `frontend/src/App.tsx` has `<Route path="/planos" element={<PlanosPage />} />`
- Route is NOT wrapped in ProtectedRoute or AdminRoute
- TypeScript compiles without errors
  </acceptance_criteria>
  <done>/planos route exists, is public (no auth), renders PlanosPage aggregator</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` passes
- `grep -r "ProtectedRoute\|AdminRoute" frontend/src/pages/public/PlanosPage.tsx` returns nothing
- `grep 'path="/planos"' frontend/src/App.tsx` returns the route line
- `grep 'animate-pulse' frontend/src/features/planos/components/PlanosPage/PlanosPage.tsx` confirms loading skeleton
- `wc -l frontend/src/pages/public/PlanosPage.tsx` shows <= 20 lines
- `wc -l frontend/src/features/planos/components/PlanosPage/PlanosPage.tsx` shows <= 200 lines
- `wc -l frontend/src/features/planos/components/PlanCard/PlanCard.tsx` shows <= 80 lines
</verification>

<success_criteria>
1. /planos page is accessible without authentication
2. Cards show name, price (BRL formatted), interval, credits, and "Assinar" CTA
3. Loading state shows skeleton cards
4. Empty state shows friendly message
5. Layout is mobile-first: 1 col mobile, 2 col tablet, 3 col desktop
6. 3-layer architecture: page <= 20L, aggregator <= 200L, micro-module <= 80L
</success_criteria>

<output>
After completion, create `.planning/phases/06-pagina-de-planos/06-02-SUMMARY.md`
</output>
