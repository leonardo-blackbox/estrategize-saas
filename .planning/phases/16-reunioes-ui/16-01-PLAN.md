---
phase: 16-reunioes-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/api/meetings.ts
  - backend/src/routes/meetings.ts
autonomous: true
requirements:
  - MEET-06

must_haves:
  truths:
    - "GET /api/meetings?consultancy_id={id} retorna array de MeetingSession filtrado por consultoria"
    - "POST /api/meetings cria sessão Recall.ai e persiste status pending"
    - "Frontend consegue chamar listMeetings(consultancyId) e createMeeting({meeting_url, consultancy_id}) via client"
    - "Tipos MeetingSession e CreateMeetingPayload exportados de frontend/src/api/meetings.ts"
  artifacts:
    - path: "frontend/src/api/meetings.ts"
      provides: "API client functions + TypeScript types para meeting_sessions"
      exports:
        - "MeetingSession"
        - "CreateMeetingPayload"
        - "listMeetings"
        - "createMeeting"
        - "meetingKeys"
    - path: "backend/src/routes/meetings.ts"
      provides: "GET / com filtro por consultancy_id"
      contains: "consultancy_id"
  key_links:
    - from: "frontend/src/api/meetings.ts listMeetings"
      to: "GET /api/meetings?consultancy_id={id}"
      via: "client.get com query param"
      pattern: "consultancy_id"
    - from: "frontend/src/api/meetings.ts createMeeting"
      to: "POST /api/meetings"
      via: "client.post com json body"
      pattern: "meeting_url"
---

<objective>
Adicionar filtro por consultancy_id no endpoint GET /api/meetings do backend e criar o módulo frontend/src/api/meetings.ts com tipos TypeScript e funções de API para a tabela meeting_sessions.

Purpose: O ConsultoriaDetailMeetings (Plan 02) depende dessas funções e tipos. Separá-los em um módulo dedicado evita poluir consultancies.ts com lógica de Recall.ai.
Output: backend filtra meetings por consultoria; frontend tem client completo pronto para consumo.
</objective>

<execution_context>
@/Users/leonardorodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leonardorodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend/src/api/client.ts
@frontend/src/api/consultancies.ts
@backend/src/routes/meetings.ts
@backend/src/database/migrations/027_meeting_sessions.sql
@backend/src/database/migrations/028_meeting_transcript_columns.sql
</context>

<interfaces>
<!-- Contratos extraídos do backend e do codebase -->

Backend route atual — backend/src/routes/meetings.ts:
```typescript
// POST / — body: { meeting_url: string, consultancy_id?: string }
// Returns: { session: MeetingSession }

// GET / — atualmente lista TODOS por user_id sem filtro de consultoria
// Precisa adicionar filtro: ?consultancy_id=UUID
// Returns: { sessions: MeetingSession[] }
```

Schema da tabela (migration 027 + 028):
```sql
meeting_sessions:
  id uuid PRIMARY KEY
  user_id uuid
  consultancy_id uuid (nullable)
  recall_bot_id text
  meeting_url text
  bot_name text DEFAULT 'Iris AI Notetaker'
  status text CHECK IN ('pending','joining','in_call','processing','done','error')
  started_at timestamptz (nullable)
  ended_at timestamptz (nullable)
  formatted_transcript text (nullable)  -- migration 028
  summary text (nullable)               -- migration 028
  speakers text[] DEFAULT '{}'          -- migration 028
  created_at timestamptz
  updated_at timestamptz
```

client.ts interface:
```typescript
export const client = {
  get:    (path: string, opts?: RequestOptions) => makeRequest('GET',    path, opts),
  post:   (path: string, opts?: RequestOptions) => makeRequest('POST',   path, opts),
  // ...
};
// opts.json → body serializado como JSON
// opts.headers → headers adicionais
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Backend — adicionar filtro consultancy_id no GET /api/meetings</name>
  <files>backend/src/routes/meetings.ts</files>
  <read_first>
    - backend/src/routes/meetings.ts (ler completo — 73 linhas)
    - backend/src/database/migrations/027_meeting_sessions.sql (schema de referência)
  </read_first>
  <action>
    Modificar o handler GET / em backend/src/routes/meetings.ts para aceitar query param consultancy_id opcional.

    Lógica exata:
    1. Extrair consultancy_id de req.query (string | undefined).
    2. Validar com z.string().uuid().optional() — se presente e inválido, retornar 400.
    3. Construir query base: supabaseAdmin.from('meeting_sessions').select('*').eq('user_id', userId).order('created_at', { ascending: false }).
    4. Se consultancy_id presente, encadear .eq('consultancy_id', consultancy_id).
    5. Manter retorno: res.json({ sessions }).

    NÃO alterar o POST /. NÃO adicionar autenticação extra (requireAuth já está no router.use).
    NÃO remover campos existentes na query.
  </action>
  <verify>
    <automated>cd /Users/leonardorodrigues/dev/estrategize-saas/backend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - GET /api/meetings?consultancy_id={valid-uuid} filtra por consultoria
    - GET /api/meetings sem parâmetro ainda retorna todas as sessions do user
    - GET /api/meetings?consultancy_id=not-a-uuid retorna 400
    - TypeScript compila sem erros
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend — criar frontend/src/api/meetings.ts com tipos e funções</name>
  <files>frontend/src/api/meetings.ts</files>
  <read_first>
    - frontend/src/api/client.ts (interface do client — 72 linhas)
    - frontend/src/api/consultancies.ts linhas 368-377 (padrão consultancyKeys)
    - backend/src/database/migrations/027_meeting_sessions.sql
    - backend/src/database/migrations/028_meeting_transcript_columns.sql
  </read_first>
  <action>
    Criar frontend/src/api/meetings.ts com o seguinte conteúdo exato:

    1. Importar client de '../api/client.ts' (caminho relativo correto do novo arquivo).

    2. Exportar interface MeetingSession:
    ```typescript
    export interface MeetingSession {
      id: string;
      user_id: string;
      consultancy_id: string | null;
      recall_bot_id: string;
      meeting_url: string;
      bot_name: string;
      status: 'pending' | 'joining' | 'in_call' | 'processing' | 'done' | 'error';
      started_at: string | null;
      ended_at: string | null;
      formatted_transcript: string | null;
      summary: string | null;
      speakers: string[];
      created_at: string;
      updated_at: string;
    }
    ```

    3. Exportar interface CreateMeetingPayload:
    ```typescript
    export interface CreateMeetingPayload {
      meeting_url: string;
      consultancy_id?: string;
    }
    ```

    4. Exportar função listMeetings:
    ```typescript
    export function listMeetings(consultancyId: string): Promise<{ sessions: MeetingSession[] }> {
      return client.get(`/api/meetings?consultancy_id=${consultancyId}`).json();
    }
    ```

    5. Exportar função createMeeting:
    ```typescript
    export function createMeeting(payload: CreateMeetingPayload): Promise<{ session: MeetingSession }> {
      return client.post('/api/meetings', { json: payload }).json();
    }
    ```

    6. Exportar objeto meetingKeys (React Query cache keys):
    ```typescript
    export const meetingKeys = {
      all: ['meetings'] as const,
      byConsultancy: (consultancyId: string) => [...meetingKeys.all, consultancyId] as const,
    };
    ```

    IMPORTANTE: O arquivo vive em frontend/src/api/meetings.ts. O import do client é `'./client'` (mesmo diretório).
    TypeScript strict — sem any.
    Máximo 60 linhas (simples, apenas tipos + funções de transporte).
  </action>
  <verify>
    <automated>cd /Users/leonardorodrigues/dev/estrategize-saas/frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Arquivo frontend/src/api/meetings.ts existe
    - Exporta: MeetingSession, CreateMeetingPayload, listMeetings, createMeeting, meetingKeys
    - TypeScript compila sem erros
    - MeetingSession.status tem union type com 6 valores: pending|joining|in_call|processing|done|error
  </done>
</task>

</tasks>

<verification>
```bash
# Backend TypeScript compila
cd /Users/leonardorodrigues/dev/estrategize-saas/backend && npx tsc --noEmit

# Frontend TypeScript compila
cd /Users/leonardorodrigues/dev/estrategize-saas/frontend && npx tsc --noEmit

# Arquivo frontend existe com exportações corretas
grep -n "export" /Users/leonardorodrigues/dev/estrategize-saas/frontend/src/api/meetings.ts

# Backend tem filtro consultancy_id
grep -n "consultancy_id" /Users/leonardorodrigues/dev/estrategize-saas/backend/src/routes/meetings.ts
```
</verification>

<success_criteria>
- backend/src/routes/meetings.ts aceita ?consultancy_id e filtra corretamente
- frontend/src/api/meetings.ts exporta 5 símbolos: MeetingSession, CreateMeetingPayload, listMeetings, createMeeting, meetingKeys
- Ambos compilam sem erros TypeScript
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/16-reunioes-ui/16-01-SUMMARY.md`
</output>
