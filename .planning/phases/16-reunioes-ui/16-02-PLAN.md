---
phase: 16-reunioes-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - frontend/src/features/consultorias/components/ConsultoriaDetailMeetings/ConsultoriaDetailMeetings.tsx
  - frontend/src/features/consultorias/components/ConsultoriaDetailMeetings/NewMeetingModal.tsx
  - frontend/src/features/consultorias/components/ConsultoriaDetailMeetings/BotSessionCard.tsx
  - frontend/src/features/consultorias/components/ConsultoriaDetailMeetings/index.ts
  - frontend/src/features/consultorias/hooks/useMeetings.ts
  - frontend/src/pages/member/ConsultoriaDetailPage.tsx
autonomous: true
requirements:
  - MEET-06

must_haves:
  truths:
    - "Modal 'Nova Reunião' abre ao clicar o botão e tem campo URL + checkbox LGPD obrigatório"
    - "Botão 'Ativar bot' no modal só é habilitado quando URL preenchida e checkbox marcado"
    - "Status do bot atualiza automaticamente a cada 5s enquanto sessão está em estado não-terminal"
    - "Lista de reuniões mostra cada sessão com data, duração calculada, badge de status colorido"
    - "Card expandido (click para abrir) mostra transcrição formatada, resumo e speakers"
    - "Badge de status é visualmente distinto: pending=cinza, joining=amarelo, in_call=verde pulsante, processing=azul, done=verde sólido, error=vermelho"
  artifacts:
    - path: "frontend/src/features/consultorias/hooks/useMeetings.ts"
      provides: "React Query hook com polling automático e mutação de criação"
      exports: ["useMeetings"]
    - path: "frontend/src/features/consultorias/components/ConsultoriaDetailMeetings/NewMeetingModal.tsx"
      provides: "Modal com form de URL + checkbox LGPD"
      exports: ["NewMeetingModal"]
    - path: "frontend/src/features/consultorias/components/ConsultoriaDetailMeetings/BotSessionCard.tsx"
      provides: "Card colapsável com badge de status + conteúdo expandido"
      exports: ["BotSessionCard"]
    - path: "frontend/src/features/consultorias/components/ConsultoriaDetailMeetings/ConsultoriaDetailMeetings.tsx"
      provides: "Agregador: header + lista + modal integrados"
      exports: ["ConsultoriaDetailMeetings"]
  key_links:
    - from: "ConsultoriaDetailMeetings.tsx onNewMeeting prop"
      to: "ConsultoriaDetailPage.tsx linha 74"
      via: "prop callback — modal já estava referenciado como future"
      pattern: "onNewMeeting"
    - from: "useMeetings refetchInterval"
      to: "listMeetings em frontend/src/api/meetings.ts"
      via: "React Query queryFn com polling 5000ms"
      pattern: "refetchInterval"
    - from: "NewMeetingModal onSubmit"
      to: "useMeetings.createSession"
      via: "callback prop (modal é puro UI, sem API knowledge)"
      pattern: "onSubmit"
---

<objective>
Implementar os 4 componentes e 1 hook da aba Reuniões: hook useMeetings com polling, modal NewMeetingModal com validação LGPD, card BotSessionCard com expansão, e o agregador ConsultoriaDetailMeetings que orquestra tudo. Também atualizar ConsultoriaDetailPage para passar o handler de abertura do modal.

Purpose: Este é o entregável principal da Phase 16 — a UX completa que permite à consultora ativar o bot Recall.ai e acompanhar o status em tempo real.
Output: Aba Reuniões funcional com modal, lista com polling e cards expansíveis.
</objective>

<execution_context>
@/Users/leonardorodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leonardorodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@frontend/src/api/meetings.ts
@frontend/src/features/consultorias/components/ConsultoriaDetailMeetings/ConsultoriaDetailMeetings.tsx
@frontend/src/features/consultorias/components/ConsultoriaDetailMeetings/index.ts
@frontend/src/features/consultorias/components/ConsultoriaDetailPage/ConsultoriaDetailPage.tsx
@frontend/src/features/consultorias/components/ConsultoriaDetailShared/ConsultoriaDetailShared.tsx
@frontend/src/components/ui/Modal.tsx
@frontend/src/components/ui/Input.tsx
@frontend/src/components/ui/Button.tsx
@frontend/src/features/consultorias/hooks/useConsultoriaDetail.ts
</context>

<interfaces>
<!-- Tipos e contratos que o executor precisa — extraídos do Plan 01 e codebase -->

De frontend/src/api/meetings.ts (criado no Plan 01):
```typescript
export interface MeetingSession {
  id: string;
  user_id: string;
  consultancy_id: string | null;
  recall_bot_id: string;
  meeting_url: string;
  bot_name: string;
  status: 'pending' | 'joining' | 'in_call' | 'processing' | 'done' | 'error';
  started_at: string | null;
  ended_at: string | null;
  formatted_transcript: string | null;
  summary: string | null;
  speakers: string[];
  created_at: string;
  updated_at: string;
}

export function listMeetings(consultancyId: string): Promise<{ sessions: MeetingSession[] }>;
export function createMeeting(payload: CreateMeetingPayload): Promise<{ session: MeetingSession }>;
export const meetingKeys: {
  all: readonly ['meetings'];
  byConsultancy: (id: string) => readonly ['meetings', string];
};
```

Modal.tsx props:
```typescript
interface ModalProps {
  open: boolean;
  onClose: () => void;
  children: ReactNode;
  title?: string;
  size?: 'sm' | 'md' | 'lg' | 'xl';
  persistent?: boolean;
}
```

Input.tsx props:
```typescript
interface InputProps extends InputHTMLAttributes<HTMLInputElement> {
  label?: string;
  error?: string;
  hint?: string;
}
```

Button.tsx props:
```typescript
interface ButtonProps {
  variant?: 'primary' | 'secondary' | 'ghost' | 'destructive';
  size?: 'xs' | 'sm' | 'md';
  loading?: boolean;
  disabled?: boolean;
}
```

ConsultoriaDetailPage linha 74 — ponto de integração:
```tsx
{activeTab === 'meetings' && id && (
  <ConsultoriaDetailMeetings consultancyId={id} onNewMeeting={() => {/* modal future */}} />
)}
```
A prop onNewMeeting deve ser substituída para abrir o modal interno.

Padrão de CSS variables do projeto (dark mode):
- Surface: var(--bg-surface-1), var(--bg-surface-2)
- Borders: var(--border-hairline), var(--border-default)
- Text: var(--text-primary), var(--text-secondary), var(--text-tertiary)
- Accent: var(--accent)
- Radius: var(--radius-md), var(--radius-sm)

Status badges — cores Tailwind com /10 opacity (padrão Phase 02):
```
pending    → bg-slate-500/10  text-slate-400   (Aguardando)
joining    → bg-yellow-500/10 text-yellow-400  (Entrando)
in_call    → bg-green-500/20  text-green-400   (Em reunião) + animate-pulse no dot
processing → bg-blue-500/10   text-blue-400    (Processando)
done       → bg-green-500/10  text-green-500   (Concluída)
error      → bg-red-500/10    text-red-400     (Erro)
```

Estados terminais (sem polling):
```typescript
const TERMINAL = new Set(['done', 'error']);
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Hook useMeetings com polling e mutação de criação</name>
  <files>frontend/src/features/consultorias/hooks/useMeetings.ts</files>
  <read_first>
    - frontend/src/api/meetings.ts (tipos e funções — criado no Plan 01)
    - frontend/src/features/consultorias/hooks/useConsultancyDocuments.ts (padrão de hook do projeto)
    - frontend/src/features/consultorias/hooks/useConsultoriaDetail.ts (padrão queryKey)
  </read_first>
  <action>
    Criar frontend/src/features/consultorias/hooks/useMeetings.ts.

    Implementação exata (máximo 120 linhas):

    ```typescript
    import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
    import { listMeetings, createMeeting, meetingKeys, type CreateMeetingPayload } from '../../../api/meetings';

    const TERMINAL = new Set(['done', 'error']);

    export function useMeetings(consultancyId: string) {
      const qc = useQueryClient();

      const { data, isLoading, error } = useQuery({
        queryKey: meetingKeys.byConsultancy(consultancyId),
        queryFn: () => listMeetings(consultancyId),
        // Polling: ativa enquanto existir qualquer sessão não-terminal
        refetchInterval: (query) => {
          const sessions = query.state.data?.sessions ?? [];
          const hasActive = sessions.some((s) => !TERMINAL.has(s.status));
          return hasActive ? 5000 : false;
        },
      });

      const sessions = data?.sessions ?? [];

      const createMutation = useMutation({
        mutationFn: (payload: CreateMeetingPayload) => createMeeting(payload),
        onSuccess: () => {
          qc.invalidateQueries({ queryKey: meetingKeys.byConsultancy(consultancyId) });
        },
      });

      return {
        sessions,
        isLoading,
        error,
        createSession: createMutation.mutate,
        isCreating: createMutation.isPending,
        createError: createMutation.error,
      };
    }
    ```

    Sem imports desnecessários. TypeScript strict.
  </action>
  <verify>
    <automated>cd /Users/leonardorodrigues/dev/estrategize-saas/frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Arquivo existe em frontend/src/features/consultorias/hooks/useMeetings.ts
    - Exporta useMeetings(consultancyId: string)
    - refetchInterval retorna 5000 quando há sessão não-terminal, false caso contrário
    - TypeScript sem erros
  </done>
</task>

<task type="auto">
  <name>Task 2: Micro-módulos NewMeetingModal + BotSessionCard e agregador ConsultoriaDetailMeetings</name>
  <files>
    frontend/src/features/consultorias/components/ConsultoriaDetailMeetings/NewMeetingModal.tsx,
    frontend/src/features/consultorias/components/ConsultoriaDetailMeetings/BotSessionCard.tsx,
    frontend/src/features/consultorias/components/ConsultoriaDetailMeetings/ConsultoriaDetailMeetings.tsx,
    frontend/src/features/consultorias/components/ConsultoriaDetailMeetings/index.ts,
    frontend/src/pages/member/ConsultoriaDetailPage.tsx
  </files>
  <read_first>
    - frontend/src/features/consultorias/components/ConsultoriaDetailMeetings/ConsultoriaDetailMeetings.tsx (substituir implementação atual — 82 linhas de stub)
    - frontend/src/features/consultorias/components/ConsultoriaDetailMeetings/index.ts (verificar export atual)
    - frontend/src/features/consultorias/components/ConsultoriaDetailPage/ConsultoriaDetailPage.tsx (linha 74 — integração onNewMeeting)
    - frontend/src/components/ui/Modal.tsx (props da Modal)
    - frontend/src/components/ui/Input.tsx (props do Input)
    - frontend/src/components/ui/Button.tsx (props do Button)
    - frontend/src/features/consultorias/hooks/useMeetings.ts (hook criado na Task 1)
  </read_first>
  <action>
    Criar/substituir 4 arquivos. Respeitar limites de linhas: micro-módulo ≤ 80L, agregador ≤ 200L.

    ─── NewMeetingModal.tsx (micro-módulo, max 80 linhas) ───────────────────────

    Props:
    ```typescript
    interface NewMeetingModalProps {
      open: boolean;
      onClose: () => void;
      onSubmit: (url: string) => void;
      isLoading: boolean;
    }
    ```

    Implementar:
    - Usar componente `<Modal>` de '../../../../components/ui/Modal' com size="sm" e title="Nova Reunião"
    - Estado local: url (string, '') e lgpdConsent (boolean, false) via useState
    - Validação de URL: usar URL nativa `new URL(value)` em try/catch para detectar URL inválida; exibir error no Input quando inválido e campo não-vazio
    - Campo Input com label="Link da reunião", placeholder="https://meet.google.com/xxx-xxx-xxx", hint="Google Meet, Zoom ou Teams"
    - Checkbox de consentimento LGPD:
      ```tsx
      <label className="flex items-start gap-2 cursor-pointer">
        <input type="checkbox" checked={lgpdConsent} onChange={(e) => setLgpdConsent(e.target.checked)}
          className="mt-0.5 accent-[var(--accent)]" />
        <span className="text-xs text-[var(--text-secondary)] leading-relaxed">
          Confirmo que todos os participantes foram informados sobre a gravação desta reunião, conforme exigido pela LGPD.
        </span>
      </label>
      ```
    - Botão "Ativar bot" com variant="primary" disabled quando URL vazia/inválida ou !lgpdConsent, loading={isLoading}
    - onSubmit recebe url (string pura) — modal não sabe que existe consultancy_id
    - Limpar estado (url='', lgpdConsent=false) quando modal fechar (onClose)
    - Usar useEffect([open]) para reset: quando open passa de true para false, resetar estado

    ─── BotSessionCard.tsx (micro-módulo, max 80 linhas) ───────────────────────

    Props:
    ```typescript
    interface BotSessionCardProps {
      session: MeetingSession;
    }
    ```

    Implementar:
    - Estado local: expanded (boolean, false)
    - Importar MeetingSession de '../../../../api/meetings'

    Badge de status — objeto de lookup STATUS_CONFIG (módulo-level, fora do componente):
    ```typescript
    const STATUS_CONFIG: Record<MeetingSession['status'], { label: string; classes: string; pulse?: boolean }> = {
      pending:    { label: 'Aguardando',   classes: 'bg-slate-500/10 text-slate-400' },
      joining:    { label: 'Entrando',     classes: 'bg-yellow-500/10 text-yellow-400' },
      in_call:    { label: 'Em reunião',   classes: 'bg-green-500/20 text-green-400', pulse: true },
      processing: { label: 'Processando',  classes: 'bg-blue-500/10 text-blue-400' },
      done:       { label: 'Concluída',    classes: 'bg-green-500/10 text-green-500' },
      error:      { label: 'Erro',         classes: 'bg-red-500/10 text-red-400' },
    };
    ```

    Layout do card (não-expandido):
    - Container div: rounded-[var(--radius-md)] p-4 bg-[var(--bg-surface-1)] border border-[var(--border-hairline)] cursor-pointer hover:border-[var(--border-default)] transition-colors
    - Row: data (created_at formatada como "DD/MM/YYYY HH:mm") + badge de status + duração (se started_at e ended_at, calcular minutos)
    - Badge: span com classes do STATUS_CONFIG + dot pulsante se pulse=true (w-1.5 h-1.5 rounded-full bg-current animate-pulse inline-block mr-1)
    - Duração: se ended_at e started_at → Math.round((new Date(ended_at) - new Date(started_at)) / 60000) + " min"; senão "—"
    - URL truncada: text-xs text-[var(--text-tertiary)] truncate max-w-xs

    Seção expandida (expanded=true, apenas se status='done'):
    - speakers: "Participantes: " + session.speakers.join(', ') (ou "—" se vazio)
    - summary: bloco com heading "Resumo" + paragraph do texto (whitespace-pre-wrap)
    - formatted_transcript: bloco colapsável com heading "Transcrição" + pre tag com text-xs font-mono overflow-auto max-h-64
    - Se status não é 'done', não renderizar seção expandida mesmo com expanded=true

    Função formatDate local:
    ```typescript
    function formatDate(iso: string): string {
      const d = new Date(iso);
      return d.toLocaleDateString('pt-BR') + ' ' + d.toLocaleTimeString('pt-BR', { hour: '2-digit', minute: '2-digit' });
    }
    ```

    ─── ConsultoriaDetailMeetings.tsx (agregador, max 200 linhas) ──────────────

    SUBSTITUIR o arquivo existente (82 linhas de stub que usa fetchMeetings/summarizeMeeting da API antiga).

    Props (manter compatibilidade com ConsultoriaDetailPage):
    ```typescript
    interface ConsultoriaDetailMeetingsProps {
      consultancyId: string;
      onNewMeeting?: () => void;  // tornar opcional — modal agora é interno
    }
    ```

    Implementar:
    - Estado local: modalOpen (boolean, false)
    - Usar useMeetings(consultancyId) — importar de '../../hooks/useMeetings'
    - Handler handleCreate(url: string): chama createSession({ meeting_url: url, consultancy_id: consultancyId }), fecha modal em onSuccess (usar useEffect no isCreating ou checar createError)
    - Melhor padrão para fechar modal após criação: no onSuccess do mutation (via useMeetings que já invalida cache), usar um ref ou state auxiliar — MAIS SIMPLES: passar onSuccess ao createSession via mutation options. Como useMeetings expõe createSession: createMutation.mutate, passar um objeto { mutationFn: ..., onSuccess: closeModal } não é possível diretamente. Solução: no hook useMeetings, retornar também createMutationSuccess como callback opcional OU usar o padrão: após createSession, observar via useEffect([isCreating]) quando isCreating volta para false sem createError → fechar modal.

    Padrão concreto para fechar modal:
    ```typescript
    const prevCreating = useRef(false);
    useEffect(() => {
      if (prevCreating.current && !isCreating && !createError) {
        setModalOpen(false);
      }
      prevCreating.current = isCreating;
    }, [isCreating, createError]);
    ```

    Layout:
    - Header: "Reuniões ({sessions.length})" + Button "Ativar bot" size="sm" variant="primary" onClick={() => setModalOpen(true)}
    - Loading state: 3 skeleton divs (h-16 animate-pulse bg-[var(--bg-surface-1)] rounded-[var(--radius-md)])
    - Empty state: card central com emoji 🎙️, texto "Nenhuma reunião registrada.", subtexto "Ative o bot Iris AI Notetaker para entrar na reunião e transcrever automaticamente.", botão "Ativar bot em reunião"
    - Lista: sessions.map(s => <BotSessionCard key={s.id} session={s} />)
    - <NewMeetingModal open={modalOpen} onClose={() => setModalOpen(false)} onSubmit={handleCreate} isLoading={isCreating} />

    ─── index.ts ────────────────────────────────────────────────────────────────

    ```typescript
    export { ConsultoriaDetailMeetings } from './ConsultoriaDetailMeetings';
    ```

    ─── ConsultoriaDetailPage.tsx — atualização da linha 74 ────────────────────

    A linha atual é:
    ```tsx
    {activeTab === 'meetings' && id && <ConsultoriaDetailMeetings consultancyId={id} onNewMeeting={() => {/* modal future */}} />}
    ```

    Substituir por:
    ```tsx
    {activeTab === 'meetings' && id && <ConsultoriaDetailMeetings consultancyId={id} />}
    ```

    O modal é agora interno ao agregador. A prop onNewMeeting é opcional e pode ser removida do JSX.
    Não alterar mais nenhuma linha do ConsultoriaDetailPage.

    ─── Regras gerais ──────────────────────────────────────────────────────────

    - Zero `any` em todos os arquivos
    - Todos os imports com caminhos relativos corretos
    - NewMeetingModal e BotSessionCard NÃO importam um ao outro (proibição de acoplamento horizontal)
    - Consultar Modal.tsx antes de usar — importar de '../../../../components/ui/Modal'
    - Consultar Input.tsx antes de usar — importar de '../../../../components/ui/Input'
    - NÃO usar `fetchMeetings` ou `summarizeMeeting` da API antiga — a aba Reuniões agora usa meeting_sessions (Recall.ai), não a tabela meetings antiga
  </action>
  <verify>
    <automated>cd /Users/leonardorodrigues/dev/estrategize-saas/frontend && npx tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep "NewMeetingModal" frontend/src/features/consultorias/components/ConsultoriaDetailMeetings/ConsultoriaDetailMeetings.tsx → encontra
    - grep "BotSessionCard" frontend/src/features/consultorias/components/ConsultoriaDetailMeetings/ConsultoriaDetailMeetings.tsx → encontra
    - grep "useMeetings" frontend/src/features/consultorias/components/ConsultoriaDetailMeetings/ConsultoriaDetailMeetings.tsx → encontra
    - grep "STATUS_CONFIG" frontend/src/features/consultorias/components/ConsultoriaDetailMeetings/BotSessionCard.tsx → encontra
    - grep "lgpdConsent" frontend/src/features/consultorias/components/ConsultoriaDetailMeetings/NewMeetingModal.tsx → encontra
    - grep "refetchInterval" frontend/src/features/consultorias/hooks/useMeetings.ts → encontra
    - npx tsc --noEmit no frontend retorna exit code 0
  </acceptance_criteria>
  <done>
    - NewMeetingModal.tsx: modal com URL input validado + checkbox LGPD + botão desabilitado quando inválido
    - BotSessionCard.tsx: card com STATUS_CONFIG lookup, badge colorido, dot pulsante para in_call, seção expandida com transcrição/resumo
    - ConsultoriaDetailMeetings.tsx: usa useMeetings, renderiza lista de BotSessionCard, abre NewMeetingModal
    - index.ts exporta ConsultoriaDetailMeetings
    - ConsultoriaDetailPage.tsx não passa mais onNewMeeting como comentário todo
    - TypeScript sem erros em todo o frontend
  </done>
</task>

</tasks>

<verification>
```bash
# TypeScript compila sem erros
cd /Users/leonardorodrigues/dev/estrategize-saas/frontend && npx tsc --noEmit

# Verificar micro-módulos existem
ls frontend/src/features/consultorias/components/ConsultoriaDetailMeetings/

# Verificar hook existe
ls frontend/src/features/consultorias/hooks/useMeetings.ts

# Verificar STATUS_CONFIG (badge de status)
grep "STATUS_CONFIG" frontend/src/features/consultorias/components/ConsultoriaDetailMeetings/BotSessionCard.tsx

# Verificar LGPD no modal
grep "lgpdConsent\|LGPD" frontend/src/features/consultorias/components/ConsultoriaDetailMeetings/NewMeetingModal.tsx

# Verificar polling no hook
grep "refetchInterval" frontend/src/features/consultorias/hooks/useMeetings.ts
```
</verification>

<success_criteria>
- Aba Reuniões renderiza lista de sessões Recall.ai (não da tabela meetings antiga)
- Modal abre com campo URL + checkbox LGPD — botão desabilitado até ambos preenchidos
- Badge de status tem 6 cores distintas; in_call tem dot pulsante
- Cards com status 'done' expandem para mostrar resumo + transcrição
- React Query refetch a cada 5s quando sessão não-terminal existe
- Zero erros de compilação TypeScript no frontend
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/16-reunioes-ui/16-02-SUMMARY.md`
</output>
