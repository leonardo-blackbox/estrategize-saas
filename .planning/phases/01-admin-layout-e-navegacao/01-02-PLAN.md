---
phase: 01-admin-layout-e-navegacao
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/admin/AdminFormacaoPage.tsx
  - frontend/src/features/admin/components/AdminFormacaoPage/AdminFormacaoPage.tsx
  - frontend/src/features/admin/components/AdminFormacaoPage/SectionCard.tsx
  - frontend/src/features/admin/components/AdminFormacaoPage/SectionCreateModal.tsx
  - frontend/src/features/admin/components/AdminFormacaoPage/StatusDropdown.tsx
  - frontend/src/features/admin/components/AdminFormacaoPage/ManageSectionCoursesModal.tsx
  - frontend/src/features/admin/components/AdminFormacaoPage/index.ts
  - frontend/src/pages/admin/AdminTurmasPage.tsx
  - frontend/src/features/admin/components/AdminTurmasPage/AdminTurmasPage.tsx
  - frontend/src/features/admin/components/AdminTurmasPage/TurmaCard.tsx
  - frontend/src/features/admin/components/AdminTurmasPage/TurmaFormModal.tsx
  - frontend/src/features/admin/components/AdminTurmasPage/ManageTurmaStudentsModal.tsx
  - frontend/src/features/admin/components/AdminTurmasPage/index.ts
  - frontend/src/pages/admin/AdminOfertasPage.tsx
  - frontend/src/features/admin/components/AdminOfertasPage/AdminOfertasPage.tsx
  - frontend/src/features/admin/components/AdminOfertasPage/OfertaCard.tsx
  - frontend/src/features/admin/components/AdminOfertasPage/OfertaFormModal.tsx
  - frontend/src/features/admin/components/AdminOfertasPage/ManageOfertaTurmasModal.tsx
  - frontend/src/features/admin/components/AdminOfertasPage/index.ts
autonomous: true
requirements:
  - ADMN-06

must_haves:
  truths:
    - "AdminFormacaoPage, AdminTurmasPage, AdminOfertasPage render identically to before"
    - "Page files in src/pages/admin/ are max 2 lines (re-export shells)"
    - "All logic lives in features/admin/components/"
    - "Aggregator files are max 200 lines each"
    - "Micro-module files are max 80 lines each"
  artifacts:
    - path: "frontend/src/features/admin/components/AdminFormacaoPage/AdminFormacaoPage.tsx"
      provides: "Formacao page aggregator — orchestrates micro-modules"
      max_lines: 200
    - path: "frontend/src/features/admin/components/AdminFormacaoPage/SectionCard.tsx"
      provides: "Single section card with inline edit, status, reorder, delete"
      max_lines: 80
    - path: "frontend/src/features/admin/components/AdminFormacaoPage/StatusDropdown.tsx"
      provides: "Status dropdown with outside-click close"
      max_lines: 80
    - path: "frontend/src/features/admin/components/AdminFormacaoPage/ManageSectionCoursesModal.tsx"
      provides: "Modal to manage courses within a section"
      max_lines: 80
    - path: "frontend/src/features/admin/components/AdminTurmasPage/AdminTurmasPage.tsx"
      provides: "Turmas page aggregator"
      max_lines: 200
    - path: "frontend/src/features/admin/components/AdminTurmasPage/TurmaFormModal.tsx"
      provides: "Create/edit turma modal"
      max_lines: 80
    - path: "frontend/src/features/admin/components/AdminTurmasPage/ManageTurmaStudentsModal.tsx"
      provides: "Manage turma students modal"
      max_lines: 80
    - path: "frontend/src/features/admin/components/AdminOfertasPage/AdminOfertasPage.tsx"
      provides: "Ofertas page aggregator"
      max_lines: 200
    - path: "frontend/src/features/admin/components/AdminOfertasPage/OfertaFormModal.tsx"
      provides: "Create/edit oferta modal"
      max_lines: 80
    - path: "frontend/src/features/admin/components/AdminOfertasPage/ManageOfertaTurmasModal.tsx"
      provides: "Manage oferta turmas modal"
      max_lines: 80
    - path: "frontend/src/pages/admin/AdminFormacaoPage.tsx"
      provides: "Thin shell re-export"
      contains: "export.*AdminFormacaoPage.*from"
    - path: "frontend/src/pages/admin/AdminTurmasPage.tsx"
      provides: "Thin shell re-export"
      contains: "export.*AdminTurmasPage.*from"
    - path: "frontend/src/pages/admin/AdminOfertasPage.tsx"
      provides: "Thin shell re-export"
      contains: "export.*AdminOfertasPage.*from"
  key_links:
    - from: "frontend/src/pages/admin/AdminFormacaoPage.tsx"
      to: "frontend/src/features/admin/components/AdminFormacaoPage/"
      via: "re-export"
      pattern: "export.*from.*features/admin"
    - from: "frontend/src/features/admin/components/AdminFormacaoPage/AdminFormacaoPage.tsx"
      to: "SectionCard, StatusDropdown, ManageSectionCoursesModal, SectionCreateModal"
      via: "import micro-modules"
      pattern: "import.*from.*\\./"
    - from: "frontend/src/pages/admin/AdminTurmasPage.tsx"
      to: "frontend/src/features/admin/components/AdminTurmasPage/"
      via: "re-export"
      pattern: "export.*from.*features/admin"
    - from: "frontend/src/pages/admin/AdminOfertasPage.tsx"
      to: "frontend/src/features/admin/components/AdminOfertasPage/"
      via: "re-export"
      pattern: "export.*from.*features/admin"
---

<objective>
Decompose the 3 largest admin pages (AdminFormacaoPage 468L, AdminTurmasPage 452L, AdminOfertasPage 364L) from monolithic files in src/pages/admin/ into proper 3-layer architecture WITH micro-module extraction to comply with CLAUDE.md line limits (aggregator max 200L, micro-module max 80L).

Purpose: Enforce the project's componentization rules. Pure-move would leave aggregators at 468/452/364 lines — violating the 200-line cap. Each page must be decomposed into aggregator + micro-modules.
Output: 3 page aggregators (each <=200L) + micro-modules (each <=80L), 3 thin shell re-exports in pages/admin/.
</objective>

<execution_context>
@/Users/leonardorodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leonardorodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-admin-layout-e-navegacao/01-RESEARCH.md

<interfaces>
<!-- Reference pattern: how AdminHomePage was decomposed -->
From frontend/src/pages/admin/AdminHomePage.tsx (2 lines):
```typescript
// Page shell — re-exports aggregator from features/admin
export { AdminHomePage } from '../../features/admin/components/AdminHomePage/index.ts';
```

From frontend/src/features/admin/components/AdminHomePage/index.ts:
```typescript
export { AdminHomePage } from './AdminHomePage.tsx';
```

<!-- Current internal structure of the 3 pages to decompose -->

AdminFormacaoPage.tsx (468L) — internal components:
- `AdminFormacaoPage` (main, lines 19-276): state + mutations + section list rendering with inline edit
- `StatusDropdown` (lines 278-368): dropdown with outside-click, status options
- `ManageSectionCoursesModal` (lines 371-468): modal to add/remove courses from a section

AdminTurmasPage.tsx (452L) — internal components:
- `formatDate` helper (lines 22-25)
- `AdminTurmasPage` (main, lines 27-185): state + mutations + turma list rendering
- `TurmaFormModal` (lines 187-311): create/edit turma form modal
- `ManageTurmaStudentsModal` (lines 313-452): manage enrolled students modal

AdminOfertasPage.tsx (364L) — internal components:
- `AdminOfertasPage` (main, lines 21-170): state + mutations + oferta list rendering
- `OfertaFormModal` (lines 173-262): create/edit oferta form modal
- `ManageOfertaTurmasModal` (lines 264-364): manage linked turmas modal
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decompose AdminFormacaoPage (468L) into aggregator + micro-modules</name>
  <files>
    frontend/src/pages/admin/AdminFormacaoPage.tsx,
    frontend/src/features/admin/components/AdminFormacaoPage/AdminFormacaoPage.tsx,
    frontend/src/features/admin/components/AdminFormacaoPage/SectionCard.tsx,
    frontend/src/features/admin/components/AdminFormacaoPage/SectionCreateModal.tsx,
    frontend/src/features/admin/components/AdminFormacaoPage/StatusDropdown.tsx,
    frontend/src/features/admin/components/AdminFormacaoPage/ManageSectionCoursesModal.tsx,
    frontend/src/features/admin/components/AdminFormacaoPage/index.ts
  </files>
  <read_first>frontend/src/pages/admin/AdminFormacaoPage.tsx</read_first>
  <action>
Read the full 468 lines of `frontend/src/pages/admin/AdminFormacaoPage.tsx` first. Then decompose into:

**Directory:** `frontend/src/features/admin/components/AdminFormacaoPage/`

**1. Extract `StatusDropdown` (currently lines ~278-368) into `StatusDropdown.tsx`:**
- Move the `StatusDropdown` function component as-is into its own file.
- It receives props: `{ current, onSelect, onClose }` (read the source to confirm exact props).
- Adjust imports — it needs `useRef`, `useEffect`, `cn`.
- Target: <=80 lines.

**2. Extract `ManageSectionCoursesModal` (currently lines ~371-468) into `ManageSectionCoursesModal.tsx`:**
- Move the modal component as-is into its own file.
- It receives props: `{ section, onClose }` (read the source to confirm exact props).
- Adjust imports — it needs `useQuery`, `useMutation`, `useQueryClient`, `Modal`, `Button`, API functions.
- Import paths from features/ depth: `../../../../components/ui/Modal.tsx`, `../../../../api/courses.ts`, etc.
- Target: <=80 lines.

**3. Extract the section card rendering from the main component's return JSX (the `sectionList.map(...)` block) into `SectionCard.tsx`:**
- This is the card for each section in the list. Read the source to identify the exact JSX block inside the `.map()`.
- The SectionCard should receive props for: section data, editing state, callbacks (onEdit, onDelete, onStatusChange, onManageCourses, onMoveUp, onMoveDown, onStartEdit, onCancelEdit, onSaveEdit).
- The aggregator calls `<SectionCard>` inside the map, passing callbacks that call the mutations.
- Target: <=80 lines. If the card JSX is too large, simplify by keeping the callbacks in the aggregator and passing only necessary data + onX callbacks.

**4. Optionally extract create modal into `SectionCreateModal.tsx`:**
- If there is a create modal (the `showCreateModal` state suggests there is), extract it.
- Target: <=80 lines.

**5. Create the aggregator `AdminFormacaoPage.tsx`:**
- Keeps: all `useState`, `useQuery`, `useMutation`, `useQueryClient` hooks and mutation definitions.
- Keeps: the overall page layout (header, loading state, empty state, section list container).
- Imports and renders: `SectionCard`, `StatusDropdown`, `ManageSectionCoursesModal`, `SectionCreateModal`.
- Passes data down via props, receives actions via callbacks.
- Adjust all import paths — from `features/admin/components/AdminFormacaoPage/` to `src/`:
  - `../../lib/` becomes `../../../../lib/`
  - `../../api/` becomes `../../../../api/`
  - `../../components/ui/` becomes `../../../../components/ui/`
  - Micro-modules: import from `./SectionCard.tsx`, `./StatusDropdown.tsx`, etc.
- Target: <=200 lines. The aggregator orchestrates; micro-modules render.

**6. Create barrel `index.ts`:**
```typescript
export { AdminFormacaoPage } from './AdminFormacaoPage.tsx';
```

**7. Replace `frontend/src/pages/admin/AdminFormacaoPage.tsx` with:**
```typescript
// Page shell — re-exports aggregator from features/admin
export { AdminFormacaoPage } from '../../features/admin/components/AdminFormacaoPage/index.ts';
```

**8. Verify:**
```bash
cd frontend && npx tsc --noEmit
wc -l src/features/admin/components/AdminFormacaoPage/*.tsx
```
Confirm aggregator <=200L and each micro-module <=80L.

CRITICAL: Read the actual source file carefully before decomposing. The component names and line ranges in this plan are guidance based on grep analysis. The actual code structure is the source of truth. If micro-modules would exceed 80 lines, split further or simplify by moving more logic into the aggregator.
  </action>
  <verify>
    <automated>cd /Users/leonardorodrigues/dev/estrategize-saas/frontend && npx tsc --noEmit 2>&1 | head -20 && echo "---FILE SIZES---" && wc -l src/features/admin/components/AdminFormacaoPage/*.tsx</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/admin/AdminFormacaoPage.tsx is exactly 2 lines (re-export shell)
    - frontend/src/features/admin/components/AdminFormacaoPage/AdminFormacaoPage.tsx exists, <=200 lines
    - All micro-module .tsx files in the directory are <=80 lines each
    - frontend/src/features/admin/components/AdminFormacaoPage/index.ts exists
    - TypeScript compiles without errors
    - No micro-module imports another micro-module from the same directory
  </acceptance_criteria>
  <done>AdminFormacaoPage decomposed: aggregator <=200L orchestrates micro-modules <=80L each. Page shell is 2 lines. Functionality unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Decompose AdminTurmasPage (452L) into aggregator + micro-modules</name>
  <files>
    frontend/src/pages/admin/AdminTurmasPage.tsx,
    frontend/src/features/admin/components/AdminTurmasPage/AdminTurmasPage.tsx,
    frontend/src/features/admin/components/AdminTurmasPage/TurmaCard.tsx,
    frontend/src/features/admin/components/AdminTurmasPage/TurmaFormModal.tsx,
    frontend/src/features/admin/components/AdminTurmasPage/ManageTurmaStudentsModal.tsx,
    frontend/src/features/admin/components/AdminTurmasPage/index.ts
  </files>
  <read_first>frontend/src/pages/admin/AdminTurmasPage.tsx</read_first>
  <action>
Read the full 452 lines of `frontend/src/pages/admin/AdminTurmasPage.tsx` first. Then decompose into:

**Directory:** `frontend/src/features/admin/components/AdminTurmasPage/`

**1. Extract `TurmaFormModal` (currently lines ~187-311, ~125L) into `TurmaFormModal.tsx`:**
- This is the create/edit turma form modal.
- Props: `{ turma (optional for edit), courses, onClose, onSuccess }`.
- Contains internal form state, create/update mutations, submit handler.
- Adjust imports to feature depth (`../../../../`).
- Target: <=80 lines. If it exceeds 80L, move the mutations out — have the modal accept `onSubmit(formData)` callback and let the aggregator handle the mutation. This reduces the modal to pure form UI.

**2. Extract `ManageTurmaStudentsModal` (currently lines ~313-452, ~140L) into `ManageTurmaStudentsModal.tsx`:**
- This is the student management modal.
- Props: `{ turma, onClose }`.
- Contains its own queries (enrollments, users), add/remove mutations.
- Target: <=80 lines. If it exceeds 80L, split: keep the modal UI but move the enrollment list into a small `EnrollmentList.tsx` micro-module, OR simplify by having the aggregator pass pre-fetched data.

**3. Extract the turma card rendering from the `.map()` block into `TurmaCard.tsx`:**
- Read the source to find the JSX block inside `turmas.map(...)`.
- The TurmaCard receives: turma data, callbacks (onEdit, onDelete, onManageStudents).
- Target: <=80 lines.

**4. Create the aggregator `AdminTurmasPage.tsx`:**
- Keeps: `formatDate` helper, all useState hooks, useQuery for turmas/courses, delete mutation.
- Keeps: overall page layout (header + create button, loading, empty state, card grid).
- Imports and renders: `TurmaCard`, `TurmaFormModal`, `ManageTurmaStudentsModal`.
- Adjust all import paths to feature depth.
- Target: <=200 lines.

**5. Create barrel `index.ts`:**
```typescript
export { AdminTurmasPage } from './AdminTurmasPage.tsx';
```

**6. Replace page shell:**
```typescript
// Page shell — re-exports aggregator from features/admin
export { AdminTurmasPage } from '../../features/admin/components/AdminTurmasPage/index.ts';
```

**7. Verify:**
```bash
cd frontend && npx tsc --noEmit
wc -l src/features/admin/components/AdminTurmasPage/*.tsx
```

CRITICAL: The ManageTurmaStudentsModal is ~140L in the source — it MUST be split to fit the 80L cap. Options:
- (a) Extract the enrollment list rendering into a separate `EnrollmentList.tsx`
- (b) Have the aggregator pre-fetch enrollments and pass as props (simplifying the modal)
- (c) Move add/remove mutations to the aggregator, modal only renders UI with callbacks

Read the source and pick the approach that keeps each file <=80L. If a micro-module genuinely cannot be reduced below 80L without harming readability, going up to ~90L is acceptable as a pragmatic compromise — but document why in a code comment.
  </action>
  <verify>
    <automated>cd /Users/leonardorodrigues/dev/estrategize-saas/frontend && npx tsc --noEmit 2>&1 | head -20 && echo "---FILE SIZES---" && wc -l src/features/admin/components/AdminTurmasPage/*.tsx</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/admin/AdminTurmasPage.tsx is exactly 2 lines (re-export shell)
    - frontend/src/features/admin/components/AdminTurmasPage/AdminTurmasPage.tsx exists, <=200 lines
    - All micro-module .tsx files in the directory are <=80 lines each (ideally; <=90L if documented)
    - frontend/src/features/admin/components/AdminTurmasPage/index.ts exists
    - TypeScript compiles without errors
    - No micro-module imports another micro-module from the same directory
  </acceptance_criteria>
  <done>AdminTurmasPage decomposed: aggregator <=200L orchestrates micro-modules <=80L each. Page shell is 2 lines. Functionality unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Decompose AdminOfertasPage (364L) into aggregator + micro-modules</name>
  <files>
    frontend/src/pages/admin/AdminOfertasPage.tsx,
    frontend/src/features/admin/components/AdminOfertasPage/AdminOfertasPage.tsx,
    frontend/src/features/admin/components/AdminOfertasPage/OfertaCard.tsx,
    frontend/src/features/admin/components/AdminOfertasPage/OfertaFormModal.tsx,
    frontend/src/features/admin/components/AdminOfertasPage/ManageOfertaTurmasModal.tsx,
    frontend/src/features/admin/components/AdminOfertasPage/index.ts
  </files>
  <read_first>frontend/src/pages/admin/AdminOfertasPage.tsx</read_first>
  <action>
Read the full 364 lines of `frontend/src/pages/admin/AdminOfertasPage.tsx` first. Then decompose into:

**Directory:** `frontend/src/features/admin/components/AdminOfertasPage/`

**1. Extract `OfertaFormModal` (currently lines ~173-262, ~90L) into `OfertaFormModal.tsx`:**
- Create/edit oferta form modal.
- Props: `{ oferta (optional for edit), onClose, onSuccess }`.
- Contains internal form state, create/update mutations.
- Target: <=80 lines. If it exceeds, move mutations to aggregator — modal receives `onSubmit(formData)`.

**2. Extract `ManageOfertaTurmasModal` (currently lines ~264-364, ~100L) into `ManageOfertaTurmasModal.tsx`:**
- Modal to manage turmas linked to an oferta.
- Props: `{ oferta, onClose }`.
- Contains turma query, toggle logic, save mutation.
- Target: <=80 lines. If it exceeds, move the save mutation to aggregator — modal calls `onSave(selectedTurmaIds)`.

**3. Extract the oferta card rendering from the `.map()` block into `OfertaCard.tsx`:**
- Read the source to find the JSX block inside `ofertas.map(...)`.
- Receives: oferta data, callbacks (onEdit, onDelete, onManageTurmas).
- Target: <=80 lines.

**4. Create the aggregator `AdminOfertasPage.tsx`:**
- Keeps: all useState hooks, useQuery for ofertas, delete mutation.
- Keeps: overall page layout (header + create button, loading, empty state, card grid).
- Imports and renders: `OfertaCard`, `OfertaFormModal`, `ManageOfertaTurmasModal`.
- Adjust all import paths to feature depth (`../../../../`).
- Target: <=200 lines.

**5. Create barrel `index.ts`:**
```typescript
export { AdminOfertasPage } from './AdminOfertasPage.tsx';
```

**6. Replace page shell:**
```typescript
// Page shell — re-exports aggregator from features/admin
export { AdminOfertasPage } from '../../features/admin/components/AdminOfertasPage/index.ts';
```

**7. Verify:**
```bash
cd frontend && npx tsc --noEmit
wc -l src/features/admin/components/AdminOfertasPage/*.tsx
```

Same rule: read the actual source to confirm sections. If a micro-module cannot fit 80L, move data-fetching/mutations to the aggregator and have the micro-module be pure UI with callbacks.
  </action>
  <verify>
    <automated>cd /Users/leonardorodrigues/dev/estrategize-saas/frontend && npx tsc --noEmit 2>&1 | head -20 && echo "---FILE SIZES---" && wc -l src/features/admin/components/AdminOfertasPage/*.tsx</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/admin/AdminOfertasPage.tsx is exactly 2 lines (re-export shell)
    - frontend/src/features/admin/components/AdminOfertasPage/AdminOfertasPage.tsx exists, <=200 lines
    - All micro-module .tsx files in the directory are <=80 lines each
    - frontend/src/features/admin/components/AdminOfertasPage/index.ts exists
    - TypeScript compiles without errors
    - No micro-module imports another micro-module from the same directory
  </acceptance_criteria>
  <done>AdminOfertasPage decomposed: aggregator <=200L orchestrates micro-modules <=80L each. Page shell is 2 lines. Functionality unchanged.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` — zero errors
2. `cd frontend && npm run build` — builds successfully
3. All 3 page files in src/pages/admin/ are exactly 2 lines
4. All 3 aggregator files exist and are <=200 lines: `wc -l frontend/src/features/admin/components/Admin{Formacao,Turmas,Ofertas}Page/Admin*Page.tsx`
5. All micro-module files are <=80 lines: `wc -l frontend/src/features/admin/components/Admin{Formacao,Turmas,Ofertas}Page/*.tsx`
6. All barrel index.ts files exist
</verification>

<success_criteria>
- AdminFormacaoPage (468L), AdminTurmasPage (452L), AdminOfertasPage (364L) all properly decomposed
- Aggregators <=200 lines, micro-modules <=80 lines (CLAUDE.md compliance)
- Page shells in src/pages/admin/ are 2 lines each (re-exports)
- No micro-module imports another micro-module from the same feature directory
- Import paths adjusted correctly (no broken imports)
- TypeScript builds clean
- Runtime behavior identical to before
</success_criteria>

<output>
After completion, create `.planning/phases/01-admin-layout-e-navegacao/01-02-SUMMARY.md`
</output>
