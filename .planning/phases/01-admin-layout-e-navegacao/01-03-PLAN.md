---
phase: 01-admin-layout-e-navegacao
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/admin/AdminCursosPage.tsx
  - frontend/src/features/admin/components/AdminCursosPage/AdminCursosPage.tsx
  - frontend/src/features/admin/components/AdminCursosPage/CourseCard.tsx
  - frontend/src/features/admin/components/AdminCursosPage/CourseCreateModal.tsx
  - frontend/src/features/admin/components/AdminCursosPage/index.ts
  - frontend/src/pages/admin/AdminStripePage.tsx
  - frontend/src/features/admin/components/AdminStripePage/AdminStripePage.tsx
  - frontend/src/features/admin/components/AdminStripePage/WebhooksTab.tsx
  - frontend/src/features/admin/components/AdminStripePage/AuditTab.tsx
  - frontend/src/features/admin/components/AdminStripePage/index.ts
  - frontend/src/pages/admin/AdminDashboardPage.tsx
  - frontend/src/features/admin/components/AdminDashboardPage/AdminDashboardPage.tsx
  - frontend/src/features/admin/components/AdminDashboardPage/index.ts
  - frontend/src/pages/admin/AdminUsuariosPage.tsx
  - frontend/src/features/admin/components/AdminUsuariosPage/AdminUsuariosPage.tsx
  - frontend/src/features/admin/components/AdminUsuariosPage/index.ts
  - frontend/src/pages/admin/AdminNovaOfertaPage.tsx
  - frontend/src/features/admin/components/AdminNovaOfertaPage/AdminNovaOfertaPage.tsx
  - frontend/src/features/admin/components/AdminNovaOfertaPage/index.ts
autonomous: true
requirements:
  - ADMN-06

must_haves:
  truths:
    - "All 5 remaining admin pages render identically to before"
    - "All page files in src/pages/admin/ are max 2 lines (re-export shells)"
    - "All admin page logic lives in features/admin/components/"
    - "Every admin page in the project follows the 3-layer pattern"
    - "AdminCursosPage aggregator is <=200 lines with micro-modules <=80 lines"
    - "AdminStripePage aggregator is <=200 lines with micro-modules <=80 lines"
  artifacts:
    - path: "frontend/src/features/admin/components/AdminCursosPage/AdminCursosPage.tsx"
      provides: "Cursos page aggregator"
      max_lines: 200
    - path: "frontend/src/features/admin/components/AdminCursosPage/CourseCard.tsx"
      provides: "Course card with status badge, lesson count, actions"
      max_lines: 80
    - path: "frontend/src/features/admin/components/AdminStripePage/AdminStripePage.tsx"
      provides: "Stripe page aggregator with tab switcher"
      max_lines: 200
    - path: "frontend/src/features/admin/components/AdminStripePage/WebhooksTab.tsx"
      provides: "Webhook events table with filters and pagination"
      max_lines: 80
    - path: "frontend/src/features/admin/components/AdminStripePage/AuditTab.tsx"
      provides: "Audit logs table with pagination"
      max_lines: 80
    - path: "frontend/src/features/admin/components/AdminDashboardPage/AdminDashboardPage.tsx"
      provides: "Dashboard page aggregator"
      max_lines: 200
    - path: "frontend/src/features/admin/components/AdminUsuariosPage/AdminUsuariosPage.tsx"
      provides: "Usuarios page aggregator"
      max_lines: 200
    - path: "frontend/src/features/admin/components/AdminNovaOfertaPage/AdminNovaOfertaPage.tsx"
      provides: "Nova Oferta page aggregator"
      max_lines: 200
  key_links:
    - from: "frontend/src/pages/admin/"
      to: "frontend/src/features/admin/components/"
      via: "re-export pattern"
      pattern: "export.*from.*features/admin"
---

<objective>
Decompose the remaining 5 admin pages into the features/admin/ 3-layer architecture. AdminCursosPage (249L) and AdminStripePage (253L) require micro-module extraction since they exceed 200L. AdminDashboardPage (131L), AdminUsuariosPage (132L), and AdminNovaOfertaPage (96L) are under 200L and can be pure-moved.

Purpose: Complete the 3-layer compliance for ALL admin pages. After this plan, every admin page shell in src/pages/admin/ is max 2 lines, and no aggregator exceeds 200 lines.
Output: 5 page aggregators in features/admin/components/ (2 with micro-modules), 5 thin shell re-exports in pages/admin/.
</objective>

<execution_context>
@/Users/leonardorodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leonardorodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-admin-layout-e-navegacao/01-RESEARCH.md

<interfaces>
<!-- Reference pattern: how AdminHomePage was decomposed -->
From frontend/src/pages/admin/AdminHomePage.tsx (2 lines):
```typescript
// Page shell — re-exports aggregator from features/admin
export { AdminHomePage } from '../../features/admin/components/AdminHomePage/index.ts';
```

<!-- Current internal structure of pages to decompose -->

AdminCursosPage.tsx (249L) — internal structure:
- Constants: `initialForm`, `statusVariant`, `statusLabel` (lines 26-38)
- `AdminCursosPage` main component (lines 40-249): state + queries + mutations + course card grid
- Each course card is rendered inline in a `.map()` — extract as CourseCard micro-module
- Create modal is inline — extract as CourseCreateModal micro-module

AdminStripePage.tsx (253L) — internal structure:
- Constants: `STATUS_STYLE` (lines 10-15)
- Helper: `formatDate` (lines 17-22)
- `AdminStripePage` main component (lines 24-253): tab state + 2 queries + tab content
- Webhooks tab content: filters + table + pagination (~100L of JSX)
- Audit tab content: table + pagination (~60L of JSX)
- Extract each tab as a micro-module (WebhooksTab, AuditTab)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decompose AdminCursosPage (249L) and AdminStripePage (253L) with micro-modules</name>
  <files>
    frontend/src/pages/admin/AdminCursosPage.tsx,
    frontend/src/features/admin/components/AdminCursosPage/AdminCursosPage.tsx,
    frontend/src/features/admin/components/AdminCursosPage/CourseCard.tsx,
    frontend/src/features/admin/components/AdminCursosPage/CourseCreateModal.tsx,
    frontend/src/features/admin/components/AdminCursosPage/index.ts,
    frontend/src/pages/admin/AdminStripePage.tsx,
    frontend/src/features/admin/components/AdminStripePage/AdminStripePage.tsx,
    frontend/src/features/admin/components/AdminStripePage/WebhooksTab.tsx,
    frontend/src/features/admin/components/AdminStripePage/AuditTab.tsx,
    frontend/src/features/admin/components/AdminStripePage/index.ts
  </files>
  <read_first>frontend/src/pages/admin/AdminCursosPage.tsx, frontend/src/pages/admin/AdminStripePage.tsx</read_first>
  <action>
**AdminCursosPage (249 lines) — decompose into aggregator + micro-modules:**

1. Read the full source of `frontend/src/pages/admin/AdminCursosPage.tsx`.

2. Create directory `frontend/src/features/admin/components/AdminCursosPage/`.

3. **Extract `CourseCard.tsx`:** The inline `.map()` block that renders each course card. Read the source to find the JSX. The card likely shows: title, status badge, lesson count, publish/archive buttons. Props: course data + onPublish/onArchive callbacks + navigate function. Target: <=80L.

4. **Extract `CourseCreateModal.tsx`:** The modal shown when `showCreate` is true. It has the form (title, description, cover_url, banner_url). Props: `{ form, setForm, onSubmit, onClose, isPending }`. Target: <=80L. If it includes mutations, move them to aggregator — modal receives onSubmit callback.

5. **Create aggregator `AdminCursosPage.tsx`:** Keeps constants (`initialForm`, `statusVariant`, `statusLabel`), all hooks (useState, useQuery, useMutation), mutation handlers. Imports `CourseCard` and `CourseCreateModal`. Renders page layout with header, loading, empty state, card grid mapping courses to `<CourseCard>`, and `<CourseCreateModal>`. Adjust imports to feature depth (`../../../../`). Target: <=200L.

6. Create barrel `index.ts` and replace page shell (same pattern as Plan 02).

**AdminStripePage (253 lines) — decompose into aggregator + micro-modules:**

1. Read the full source of `frontend/src/pages/admin/AdminStripePage.tsx`.

2. Create directory `frontend/src/features/admin/components/AdminStripePage/`.

3. **Extract `WebhooksTab.tsx`:** The webhooks tab content — filter selects (status, provider), table of webhook events, pagination controls. Props: `{ webhooks, total, page, onPageChange, statusFilter, onStatusFilter, providerFilter, onProviderFilter, isLoading, limit }`. Target: <=80L. The tab receives pre-fetched data from aggregator.

4. **Extract `AuditTab.tsx`:** The audit logs tab content — table of audit entries, pagination. Props: `{ logs, total, page, onPageChange, isLoading, limit }`. Target: <=80L.

5. **Create aggregator `AdminStripePage.tsx`:** Keeps `STATUS_STYLE`, `formatDate`, tab state, both useQuery hooks, pagination state. Renders tab switcher + conditionally renders `<WebhooksTab>` or `<AuditTab>` passing data/callbacks as props. Adjust imports. Target: <=200L.

6. Create barrel `index.ts` and replace page shell.

**Verify both:**
```bash
cd frontend && npx tsc --noEmit
wc -l src/features/admin/components/AdminCursosPage/*.tsx
wc -l src/features/admin/components/AdminStripePage/*.tsx
```
  </action>
  <verify>
    <automated>cd /Users/leonardorodrigues/dev/estrategize-saas/frontend && npx tsc --noEmit 2>&1 | head -20 && echo "---CURSOS SIZES---" && wc -l src/features/admin/components/AdminCursosPage/*.tsx && echo "---STRIPE SIZES---" && wc -l src/features/admin/components/AdminStripePage/*.tsx</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/admin/AdminCursosPage.tsx is exactly 2 lines (re-export shell)
    - frontend/src/pages/admin/AdminStripePage.tsx is exactly 2 lines (re-export shell)
    - AdminCursosPage aggregator <=200L, CourseCard <=80L, CourseCreateModal <=80L
    - AdminStripePage aggregator <=200L, WebhooksTab <=80L, AuditTab <=80L
    - Both index.ts barrel files exist
    - TypeScript compiles without errors
    - No micro-module imports another micro-module from the same directory
  </acceptance_criteria>
  <done>AdminCursosPage and AdminStripePage decomposed with proper micro-modules. Aggregators <=200L, micro-modules <=80L. Page shells are 2 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Move AdminDashboardPage (131L), AdminUsuariosPage (132L), AdminNovaOfertaPage (96L) — pure move</name>
  <files>
    frontend/src/pages/admin/AdminDashboardPage.tsx,
    frontend/src/features/admin/components/AdminDashboardPage/AdminDashboardPage.tsx,
    frontend/src/features/admin/components/AdminDashboardPage/index.ts,
    frontend/src/pages/admin/AdminUsuariosPage.tsx,
    frontend/src/features/admin/components/AdminUsuariosPage/AdminUsuariosPage.tsx,
    frontend/src/features/admin/components/AdminUsuariosPage/index.ts,
    frontend/src/pages/admin/AdminNovaOfertaPage.tsx,
    frontend/src/features/admin/components/AdminNovaOfertaPage/AdminNovaOfertaPage.tsx,
    frontend/src/features/admin/components/AdminNovaOfertaPage/index.ts
  </files>
  <read_first>frontend/src/pages/admin/AdminDashboardPage.tsx, frontend/src/pages/admin/AdminUsuariosPage.tsx, frontend/src/pages/admin/AdminNovaOfertaPage.tsx</read_first>
  <action>
These 3 pages are under 200L, so they can be pure-moved (no micro-module extraction needed). For each page:

**AdminDashboardPage (131 lines):**

1. Read full content of `frontend/src/pages/admin/AdminDashboardPage.tsx`.
2. Create directory `frontend/src/features/admin/components/AdminDashboardPage/`.
3. Move ENTIRE content to `frontend/src/features/admin/components/AdminDashboardPage/AdminDashboardPage.tsx`. Adjust relative imports — new file is 2 levels deeper:
   - `../../` becomes `../../../../`
   - Check every import path.
4. Create `frontend/src/features/admin/components/AdminDashboardPage/index.ts`:
   ```typescript
   export { AdminDashboardPage } from './AdminDashboardPage.tsx';
   ```
5. Replace `frontend/src/pages/admin/AdminDashboardPage.tsx` with:
   ```typescript
   // Page shell — re-exports aggregator from features/admin
   export { AdminDashboardPage } from '../../features/admin/components/AdminDashboardPage/index.ts';
   ```

**AdminUsuariosPage (132 lines):**

1. Read full content.
2. Create directory `frontend/src/features/admin/components/AdminUsuariosPage/`.
3. Move content to `frontend/src/features/admin/components/AdminUsuariosPage/AdminUsuariosPage.tsx`. Adjust imports (2 levels deeper).
4. Create `frontend/src/features/admin/components/AdminUsuariosPage/index.ts`:
   ```typescript
   export { AdminUsuariosPage } from './AdminUsuariosPage.tsx';
   ```
5. Replace page shell:
   ```typescript
   // Page shell — re-exports aggregator from features/admin
   export { AdminUsuariosPage } from '../../features/admin/components/AdminUsuariosPage/index.ts';
   ```

**AdminNovaOfertaPage (96 lines):**

1. Read full content.
2. Create directory `frontend/src/features/admin/components/AdminNovaOfertaPage/`.
3. Move content to `frontend/src/features/admin/components/AdminNovaOfertaPage/AdminNovaOfertaPage.tsx`. Adjust imports (2 levels deeper).
4. Create `frontend/src/features/admin/components/AdminNovaOfertaPage/index.ts`:
   ```typescript
   export { AdminNovaOfertaPage } from './AdminNovaOfertaPage.tsx';
   ```
5. Replace page shell:
   ```typescript
   // Page shell — re-exports aggregator from features/admin
   export { AdminNovaOfertaPage } from '../../features/admin/components/AdminNovaOfertaPage/index.ts';
   ```

6. Verify TypeScript compiles: `cd frontend && npx tsc --noEmit`

7. **Final validation:** Run `wc -l frontend/src/pages/admin/*.tsx` and confirm ALL page files are now max 2 lines.
  </action>
  <verify>
    <automated>cd /Users/leonardorodrigues/dev/estrategize-saas/frontend && npx tsc --noEmit 2>&1 | head -20 && echo "---PAGE SIZES---" && wc -l src/pages/admin/*.tsx</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/admin/AdminDashboardPage.tsx is exactly 2 lines
    - frontend/src/pages/admin/AdminUsuariosPage.tsx is exactly 2 lines
    - frontend/src/pages/admin/AdminNovaOfertaPage.tsx is exactly 2 lines
    - All 3 aggregator files exist in features/admin/components/ with respective export functions
    - All 3 index.ts barrel files exist
    - TypeScript compiles without errors
    - ALL files in src/pages/admin/ are max 2 lines
  </acceptance_criteria>
  <done>All remaining admin pages decomposed. Every page file in src/pages/admin/ is a 2-line re-export shell. Full 3-layer compliance achieved for the entire admin area.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` — zero errors
2. `cd frontend && npm run build` — builds successfully
3. `wc -l frontend/src/pages/admin/*.tsx` — every file is 2 lines
4. `wc -l frontend/src/features/admin/components/AdminCursosPage/*.tsx` — aggregator <=200L, micro-modules <=80L
5. `wc -l frontend/src/features/admin/components/AdminStripePage/*.tsx` — aggregator <=200L, micro-modules <=80L
6. All aggregator files exist in features/admin/components/ with named exports
7. No broken imports (TypeScript catches this)
</verification>

<success_criteria>
- All 5 pages decomposed into features/admin/components/
- AdminCursosPage and AdminStripePage have proper micro-module extraction (aggregator <=200L, micro-modules <=80L)
- AdminDashboardPage, AdminUsuariosPage, AdminNovaOfertaPage pure-moved (already <=200L)
- All page shells in src/pages/admin/ are 2 lines
- Combined with Plan 02, ALL admin pages now follow the 3-layer pattern with line limits
- TypeScript builds clean
- Runtime behavior identical to before
</success_criteria>

<output>
After completion, create `.planning/phases/01-admin-layout-e-navegacao/01-03-SUMMARY.md`
</output>
