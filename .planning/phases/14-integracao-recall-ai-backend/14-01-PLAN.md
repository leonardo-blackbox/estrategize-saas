---
phase: 14-integracao-recall-ai-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/database/migrations/027_meeting_sessions.sql
  - backend/src/services/recallService.ts
  - backend/src/routes/meetings.ts
  - backend/src/app.ts
autonomous: true
requirements: [MEET-01, MEET-02]
user_setup:
  - service: recall-ai
    why: "Bot de transcrição de reuniões"
    env_vars:
      - name: RECALL_API_KEY
        source: "Recall.ai Dashboard -> API Keys"
      - name: RECALL_WEBHOOK_SECRET
        source: "Recall.ai Dashboard -> Webhooks -> Signing Secret"
    dashboard_config:
      - task: "Configurar webhook URL apontando para POST {BACKEND_URL}/api/webhooks/recall"
        location: "Recall.ai Dashboard -> Webhooks"

must_haves:
  truths:
    - "Migration 027 cria tabelas meeting_sessions e meeting_transcripts com RLS"
    - "POST /api/meetings/bot aceita meeting_url e cria bot no Recall.ai"
    - "Sessão é salva no banco com status pending e recall_bot_id"
  artifacts:
    - path: "backend/src/database/migrations/027_meeting_sessions.sql"
      provides: "Tabelas meeting_sessions e meeting_transcripts"
      contains: "CREATE TABLE"
    - path: "backend/src/services/recallService.ts"
      provides: "Client HTTP para Recall.ai API"
      exports: ["createBot"]
    - path: "backend/src/routes/meetings.ts"
      provides: "POST /bot endpoint autenticado"
      exports: ["default"]
  key_links:
    - from: "backend/src/routes/meetings.ts"
      to: "backend/src/services/recallService.ts"
      via: "createBot()"
      pattern: "createBot"
    - from: "backend/src/routes/meetings.ts"
      to: "supabaseAdmin"
      via: "insert meeting_sessions"
      pattern: "meeting_sessions"
    - from: "backend/src/app.ts"
      to: "backend/src/routes/meetings.ts"
      via: "app.use mount"
      pattern: "meetingsRouter"
---

<objective>
Criar a fundação de banco de dados e o endpoint para criação de bot Recall.ai em reuniões.

Purpose: Permitir que consultoras enviem um link de reunião e o sistema crie um bot que entra na chamada para transcrever.
Output: Migration 027, recallService.ts, meetings.ts route, app.ts atualizado.
</objective>

<execution_context>
@/Users/leonardorodrigues/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leonardorodrigues/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@backend/src/app.ts
@backend/src/middleware/auth.ts
@backend/src/database/migrations/026_knowledge_base.sql
@backend/src/lib/supabaseAdmin.ts

<interfaces>
<!-- Existing patterns executor needs -->

From backend/src/middleware/auth.ts:
```typescript
export interface AuthenticatedRequest extends Request {
  userId?: string;
}
export async function requireAuth(req: AuthenticatedRequest, res: Response, next: NextFunction): Promise<void>;
```

From backend/src/app.ts (mount pattern):
```typescript
app.use('/api/consultancies', consultanciesRouter);
app.use('/api/webhooks', webhookLimit, webhooksRouter);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 027 — meeting_sessions e meeting_transcripts</name>
  <files>backend/src/database/migrations/027_meeting_sessions.sql</files>
  <read_first>backend/src/database/migrations/026_knowledge_base.sql</read_first>
  <action>
Criar migration 027_meeting_sessions.sql com as duas tabelas:

**meeting_sessions:**
```sql
CREATE TABLE IF NOT EXISTS public.meeting_sessions (
  id              uuid        DEFAULT gen_random_uuid() PRIMARY KEY,
  user_id         uuid        NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  consultancy_id  uuid        REFERENCES public.consultancies(id) ON DELETE SET NULL,
  recall_bot_id   text        NOT NULL,
  meeting_url     text        NOT NULL,
  bot_name        text        NOT NULL DEFAULT 'Iris AI Notetaker',
  status          text        NOT NULL DEFAULT 'pending' CHECK (status IN ('pending', 'joining', 'in_call', 'processing', 'done', 'error')),
  started_at      timestamptz,
  ended_at        timestamptz,
  created_at      timestamptz DEFAULT now(),
  updated_at      timestamptz DEFAULT now()
);
```

**meeting_transcripts:**
```sql
CREATE TABLE IF NOT EXISTS public.meeting_transcripts (
  id              uuid        DEFAULT gen_random_uuid() PRIMARY KEY,
  session_id      uuid        NOT NULL REFERENCES public.meeting_sessions(id) ON DELETE CASCADE,
  speaker         text        NOT NULL DEFAULT 'unknown',
  words           jsonb       NOT NULL DEFAULT '[]',
  raw_text        text        NOT NULL DEFAULT '',
  timestamp       timestamptz DEFAULT now(),
  created_at      timestamptz DEFAULT now()
);
```

**Indexes:**
- idx_meeting_sessions_user ON meeting_sessions(user_id)
- idx_meeting_sessions_consultancy ON meeting_sessions(consultancy_id) WHERE consultancy_id IS NOT NULL
- idx_meeting_sessions_recall_bot ON meeting_sessions(recall_bot_id)
- idx_meeting_transcripts_session ON meeting_transcripts(session_id)

**RLS (ambas tabelas ENABLE ROW LEVEL SECURITY):**

meeting_sessions:
- SELECT: auth.uid() = user_id
- INSERT: auth.uid() = user_id
- UPDATE: auth.uid() = user_id
- DELETE: auth.uid() = user_id

meeting_transcripts (via session ownership):
- SELECT: EXISTS (SELECT 1 FROM meeting_sessions s WHERE s.id = session_id AND s.user_id = auth.uid())
- INSERT: EXISTS (SELECT 1 FROM meeting_sessions s WHERE s.id = session_id AND s.user_id = auth.uid())

**updated_at trigger** para meeting_sessions (mesmo padrão de 026).
  </action>
  <verify>
    <automated>[ $(grep -c "CREATE TABLE" backend/src/database/migrations/027_meeting_sessions.sql) -eq 2 ] && [ $(grep -c "ENABLE ROW LEVEL SECURITY" backend/src/database/migrations/027_meeting_sessions.sql) -eq 2 ] && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- Migration file exists at backend/src/database/migrations/027_meeting_sessions.sql
- Contains CREATE TABLE for meeting_sessions and meeting_transcripts
- Both tables have ENABLE ROW LEVEL SECURITY
- RLS policies reference auth.uid() = user_id
- meeting_transcripts RLS uses session ownership subquery
- status CHECK constraint includes: pending, joining, in_call, processing, done, error
- recall_bot_id is text NOT NULL
- words column is jsonb NOT NULL DEFAULT '[]'
  </acceptance_criteria>
  <done>Migration 027 com 2 tabelas, indexes, RLS e trigger de updated_at</done>
</task>

<task type="auto">
  <name>Task 2: recallService + POST /api/meetings/bot endpoint + app.ts wiring</name>
  <files>backend/src/services/recallService.ts, backend/src/routes/meetings.ts, backend/src/app.ts</files>
  <read_first>backend/src/app.ts, backend/src/middleware/auth.ts, backend/src/routes/consultancyDocuments.ts</read_first>
  <action>
**1. backend/src/services/recallService.ts** (max 100 linhas):

```typescript
const RECALL_API_URL = 'https://us-east-1.recall.ai/api/v1';

interface CreateBotParams {
  meetingUrl: string;
  botName?: string;
}

interface RecallBot {
  id: string;
  status: string;
}

export async function createBot(params: CreateBotParams): Promise<RecallBot> {
  const apiKey = process.env.RECALL_API_KEY;
  if (!apiKey) throw new Error('RECALL_API_KEY not configured');

  const res = await fetch(`${RECALL_API_URL}/bot`, {
    method: 'POST',
    headers: {
      'Authorization': `Token ${apiKey}`,
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({
      meeting_url: params.meetingUrl,
      bot_name: params.botName ?? 'Iris AI Notetaker',
      transcription_options: { provider: 'assembly_ai' },
    }),
  });

  if (!res.ok) {
    const text = await res.text();
    throw new Error(`Recall.ai API error ${res.status}: ${text}`);
  }

  return res.json() as Promise<RecallBot>;
}
```

**2. backend/src/routes/meetings.ts** (max 120 linhas):

- Import: Router, requireAuth, AuthenticatedRequest, supabaseAdmin, createBot
- `const router = Router();`
- Todas as rotas com requireAuth

**POST /** (create bot):
- Zod schema: `{ meeting_url: z.string().url(), consultancy_id: z.string().uuid().optional() }`
- Chama createBot({ meetingUrl, botName: 'Iris AI Notetaker' })
- Insere em meeting_sessions: user_id (req.userId), consultancy_id, recall_bot_id (bot.id), meeting_url, bot_name, status: 'pending'
- Retorna 201 com { session }
- try/catch: se Recall falhar, retorna 502 com { error: 'Failed to create meeting bot' }

**GET /** (list sessions):
- Busca meeting_sessions WHERE user_id = req.userId, ORDER BY created_at DESC
- Retorna 200 com { sessions }

**3. backend/src/app.ts:**
- Adicionar import: `import meetingsRouter from './routes/meetings.js';`
- Adicionar mount: `app.use('/api/meetings', authLimit, meetingsRouter);`
- Posicionar ANTES da linha de webhooks (junto com outras rotas autenticadas)

Usar authLimit (não adminLimit) pois meetingsRouter serve endpoints de membro.
  </action>
  <verify>
    <automated>grep -q "createBot" backend/src/services/recallService.ts && grep -q "meetingsRouter" backend/src/app.ts && grep -q "recall_bot_id" backend/src/routes/meetings.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- recallService.ts exports createBot function
- recallService.ts uses RECALL_API_KEY env var with Token auth header
- recallService.ts sends transcription_options with assembly_ai provider
- meetings.ts has POST / with Zod validation for meeting_url (url) and consultancy_id (optional uuid)
- meetings.ts has GET / listing user sessions
- meetings.ts uses requireAuth middleware
- meetings.ts inserts into meeting_sessions with status 'pending'
- app.ts imports and mounts meetingsRouter at /api/meetings with authLimit
- No TypeScript any usage
  </acceptance_criteria>
  <done>Endpoint POST /api/meetings/bot cria bot Recall.ai e salva sessão; GET /api/meetings lista sessões do usuário; app.ts wired</done>
</task>

</tasks>

<verification>
1. Migration 027 tem 2 CREATE TABLE, 2 ENABLE ROW LEVEL SECURITY, indexes e trigger
2. recallService.ts exporta createBot com RECALL_API_KEY
3. meetings.ts tem POST e GET com requireAuth
4. app.ts importa e monta meetingsRouter em /api/meetings
</verification>

<success_criteria>
- Migration 027 pronta para aplicar com meeting_sessions e meeting_transcripts + RLS
- POST /api/meetings/bot cria bot via Recall.ai API e persiste sessão com status pending
- GET /api/meetings retorna sessões do usuário autenticado
- Todas as rotas protegidas por requireAuth
</success_criteria>

<output>
After completion, create `.planning/phases/14-integracao-recall-ai-backend/14-01-SUMMARY.md`
</output>
